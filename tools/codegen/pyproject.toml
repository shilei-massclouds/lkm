[project]
name = "lkm-codegen"
version = "0.1.0"
description = "Generate implementation artifacts from LKM model intermediate JSON."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
lkm-codegen = "codegen_tool.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
