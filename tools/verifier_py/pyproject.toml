[project]
name = "verifier-py"
version = "0.1.0"
description = "Python static derivation verifier for LKM object model specs."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
