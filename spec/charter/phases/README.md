# Charter Phases

Phase-level charter notes live here when they need a dedicated topic file.

- [`BootInitFlow`](boot-init-flow.md) 是该具体 TaskFlow 实例的唯一 phase-level Charter。
- [`boot/`](boot/) 只保留 boot 叶阶段 namespace 导航，不声明聚合对象或核心语义。
