/*
 * Entry Successor Phase Specification
 *
 * Placeholder for the formal model described by:
 * - spec/pic/入口后继期的各个对象.svg
 * - spec/pic/入口后继期对象构建时序.svg
 *
 * The first formal version should model EntrySuccessorPhase following the
 * documented sequence from InitStack.enable() through MemBlock.enable(),
 * VM.enable(), and EarlyDtb.cleanup().
 */
