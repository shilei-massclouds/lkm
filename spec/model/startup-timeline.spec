/*
 * Startup Timeline Specification
 *
 * This is the top-level model entry. It follows the phase tree layout under
 * spec/model and currently includes the existing entry-prelude formal model
 * through the boot phase wrapper.
 */

include "boot/phase.spec";
