# 当前上下文归档

> 本文从 `docs/ROADMAP.md` 拆出，只保留专题历史、证据和细节。当前优先级、状态和下一步顺序以 [`../ROADMAP.md`](../ROADMAP.md) 为唯一入口。

## Roadmap 拆分策略

- `docs/ROADMAP.md` 只保留仍有后续责任的活跃任务，是唯一优先级和状态入口。
- `docs/roadmap/*.md` 只保存专题历史、证据、长说明和交接细节；其中的计划/优先级措辞是历史快照。
- 无剩余责任的任务从主表移出，完整证据留在唯一专题；`completed.md` 只保存领域、任务名和专题链接。
- 活跃任务更新主 roadmap；完成迁移在同一变更中同步专题证据和中央索引。

## 当前焦点

1. 当前最高优先级仍是内部可见性驱动的缺陷复现、回归和差分诊断。stress runner、普通 DF-0001/DF-0002 case 和 `stress-mem` 低扰动入口已经完成首轮；DF-0001 原始 `/sbin/init` 读取失败已归档并保留回归，DF-0002 已从全局 PLIC counter 过强观察约束收敛到 source-scoped 判据并转入长期 stress 回归。`Serial8250RxBatchLoopbackProbe.setup` 内部 first-failed predicate 已补齐到 source claim/complete delta matched，补齐后 DF-0001/DF-0002 两条 `stress-mem` 500 次复测均 500/500 成功。DF-0003 已从单次 `/bin/ls` 升级为连续两次 `/bin/ls`：第二次 clone 的稳定 ENOSYS 已定位为首轮 PID1 plain child wait/reap 后内部 slot/runqueue 未释放，并按 top-level child 与 observed shell 两种生命周期分别修复；双命令 ordinary path 当前 30/30 stress 成功。
2. trace 策略按证据驱动推进：当前先使用 checkpoint announce、checkpoint observer、TrapType、failure diagnostic 和 stress-mem 产物支撑纵向差分；只有确认现有内部可见性不足时，再补 task/CPU/IRQ/VFS/block 相关最小观测点或 Linux-like trace 点。项目与 Linux 6.12 的横向差分仍保留为后续主要能力，但应建立在本项目内部可见性和纵向差分闭环之后。
3. PLIC 驱动、UART 外部中断链和 serial8250 interrupt-driven console TX 当前轮已阶段性收尾：irqchip/irqdomain、PLIC 独立驱动对象、设备树驱动的 PLIC setup、platform IRQ resource 解析、serial8250 IRQ 绑定、handler registry、中断上下文约束、root INTC -> PLIC -> UART handler dispatch、一轮 UART THRE interrupt cycle 的 claim/complete/zero-claim 闭合，以及 `printk` 前端提交后由 THRI 中断驱动 UART handler drain TX queue 的 runtime TX 闭环均已验证。kernel `printk` console 稳定性已覆盖连续/批量 printk burst、Linux-like `tx_loadsz` 预算、单条长 printk 多轮 drain、多条长 printk long-burst，以及 long-burst 后的 `Serial8250ConsoleTxQuiesceProbe`；固定 burst/long-burst 和 ordinary TTY write `W`/`tx04` 等用户可见 TX/TTY 测试负载已经收口到 checkpoint/KUnit 测试构建，普通 `make run` 不再打印固定探针字节。
4. 本项目与 Linux 6.12 的交叉验证首轮已完成 PLIC 二进制复用收尾：`drivers/irqchip/irq-sifive-plic.o` 已能在 `arceos_ex` 中作为 Linux object provider 承接 UART 外部中断主线，并与 native provider 收敛到同一薄 provider contract。参考源码位置为 `../linux-6.12`；后续不再主动扩展 PLIC deferred 项，除非新目标实际踩到。
5. 维持 `ioremap/vmalloc` 当前首轮成果：runtime page-table metadata 已覆盖完整 `VMALLOC_START..VMALLOC_END`，支持按需追加 L1/L0/PTE 页表页，`VmapArea` / `VmapMapping` 记录已改为动态容器 backing；更完整的 `vm_struct/vmap_area` 行为，例如空洞复用、增强树查找、lazy purge 和并发边界，降为后续补强，除非 nightly/压力复现或纵向差分证据显示其阻塞 IRQ/driver 路径的一致性。
6. serial8250 runtime RX/TTY/FIFO 补强继续保留为后续项。当前对象框架和 transition/action 已确认，RX 单字符/批量 loopback、ordinary TTY TX FIFO 最小 enqueue/dequeue，以及 ordinary TTY write 经 `TtyXmitFifo -> StartTx/THRI -> handler TransmitChars` 的单 byte 和固定小批量真实 IRQ 闭合均已完成；后续是否继续展开 printk TX 异常/压力边界、TTY runtime 分层或用户态 I/O，应先经过 nightly/压力复现和纵向 trace 证据排序。
7. `virtio-rng` 与 virtio 基础对象首轮闭环已完成：`virtio-mmio` platform driver、`VirtioBus` / `VirtioDevice`、split `VirtQueue`、真实 QEMU `virtio-rng-device` completion、Linux-like `HwRngCore` / `HwRngDevice` 和 `virtrng_scan` / read 边界均已验证。`virtio-blk` 已推进到真实 QEMU `virtio-blk-device` 发现/config/单队列 setup/DRIVER_OK、最小 read request、Linux-like `BlockDeviceRegistry` / major-minor 可发现对象、经 `Bio` / `submit_bio_wait` / `BufferHead` / `sb_bread` 完成真实块读，以及 read-only ext2 显式 mount/root lookup/direct-block file read；4K Buffer / 4K ext2 block 支持和 Ext2 多 direct-block read path 泛化已完成。
8. 用户态 `helloworld` 已推进到 dynamic musl libc `/sbin/init` 综合 smoke：当前能从 Alpine ext2 rootfs 读取构造期 overlay 的 `user_smoke` 动态链接产物，识别 `PT_INTERP=/lib/ld-musl-riscv64.so.1`，读取并映射 musl interpreter，构造动态链接器所需 auxv，经 `KernelInitTask`/`PayloadPhase` 建立 `UserAddressSpace` / `UserTrapFrame`，写入 `satp` 并 `sret` 进入 U-mode。真实路径已支持 `set_tid_address`、`brk/mmap/mprotect/munmap`、`writev` 错误输出路径、read-only `openat/read/close/newfstatat`、directory `openat/getdents64`、`readlinkat` fast-symlink 读取和 `write/exit`，并按页粒度接受 GNU_RELRO `mprotect`；默认 overlay 已切到 `user_smoke musl dynamic`，运行输出逐项 `syscall ... ok`、`user hello` 和 `user exit status=0`。page cache、完整 TTY/N_TTY、Ext2 写路径、double/triple indirect、完整 VMA tree 和发行版 init/sh 仍是后续项。
9. 当前 `/bin/sh -> /bin/ls` P0 已越过 BusyBox job-control/TTY 和 child `execve(221)` 边界：plain fork、parent `wait4(260)` handoff、bounded child stack snapshot、child pgrp/job-control 首片、execve staging address space 和 start_thread trap-frame 改写已经闭合。2026-07-16 固定输入 `/bin/ls\n/bin/ls\nexit\n` 进一步发现首轮 child wait/reap 后 `UserChild` 仍为 `Ready + enqueued + in-runqueue`，`parent_wait_resumed=1` 使第二次 clone 错入 `copy_user_process` 并返回 ENOSYS；修复在 top-level plain child status copyout 后出队并将 slot 复位 Prepared，递增 user-visible pid 后复用 internal task ref。observed shell grandchild 则保持 Ready/enqueued shell，只清理上一轮临时 continuation/snapshot/wait/exit 状态。对象 smoke、DF-0003 30/30、默认完整 stress 三 case 各 10/10、Linux exact paired baseline 1/1 和默认 rc.local difftest 1/1 均通过；exact case 的 arceos_ex stress-mem 提升到 256 KiB 后完整保留两组 UserExec checkpoint，双方 `first_divergence=null` 且无 overflow。完整 clone3、CLONE_VM/vfork、线程组、COW mm、完整 wait sleep/wakeup/reap/status copyout、setsid/orphan pgrp/pty/job-control signal、exec old-mm 回收/失败回滚/CLOEXEC/credentials/signal/binfmt point-of-no-return 等仍 deferred。
10. 交互式 shell prompt-return 诊断主线已完成当前轮闭合：用户手工 `make run APP=user-boot` 进入 `/bin/sh` 后输入 `ls`，曾观察到 `wait4 child handoff` 和目录输出后 guest 退出而非回到 `/ #` prompt；本轮通过 `SyscallTable.Wait4` / `UserChild.ParentWaitResumed` checkpoint、parent wait 栈窗口比较、writable page checksum summary 和 BusyBox objdump，把边界定位为缺少 Linux `dup_mm()`/COW 隔离导致 child fork/exec 路径污染 parent writable 页。当前首片按 Linux 6.12 `kernel/fork.c::dup_mm()/copy_mm()` 差分分类为单 observed-child parent page rollback：wait4 handoff 前保存 parent writable user pages，child exit 恢复 parent address-space 后先记录 dirty facts，再在 wait status copyout 前恢复全部 saved writable pages 并返回 parent wait4；完整 COW mm、共享 PTE write-protect、page fault COW、mm refcount、VMA tree 和完整 wait/reap lifecycle 仍 deferred。focused PTY 已观察到 `ls` 输出后再 `exit` 为 `user exit status=0`，直接根目录 `make test` 通过 `overall total=165 pass=165 fail=0`。本轮已把 `wait4 child handoff` 详细寄存器行收口到显式 `PROBE=user-syscall-trace` 路径，默认 shell smoke 断言功能输出和 checkpoint/KUnit facts，不再依赖该诊断文本。
11. BusyBox init getty/login shell 的 bounded job-control 验收已归档。bare Alpine `ROOTFS_OVERLAY=none` 继续保留 `root:*` locked 账号状态；需要登录的验收通过显式 account-only `ROOTFS_FILE_OVERLAY_DIR` 添加专用测试用户和 test-only 密码 hash。host delayed stdin 已分阶段越过 `login:`、`Password:`、认证、单层 nested vfork takeover、fd-local `fchown(55)` / `fchmod(52)`、AF_UNIX stream socket fd 创建、`/var/run/nscd/socket` pathname `ENOENT`、root-only `setgroups(159)`、`setgid(144, gid=100)`、`setuid(146, uid=1000)` 和登录 shell `execve(221)` filename/argv bounded C-string copy；focused run 已进入 `~ $`。本片前 focused baseline 使用 account overlay、`PROBE=user-syscall-trace,user-syscall-error` 和 staged `/bin/ls\nexit\n`，确认 `/bin/ls` 的直接边界是 child continuation 内普通 `clone(220)`：`flags=0x11`、`flags_wo_csignal=0`、`exit_signal=17`、`newsp=0`、`current_child=1`、`child_pid=7`、`nested_parent_pid=6`、`active_task_record_available=0`、`completed_records_total_archived=3`、`completed_records_reaped=3`、`next_child_pid=8`，旧行为返回 `ENOSYS` 并打印 `can't fork: Function not implemented`。当前片把该形态规格化为串行化的 internal task-record storage 的 observed child plain fork：clone 记录 shell parent/grandchild continuation facts并返回 grandchild pid，父侧 pending identity 继承 shell pgrp/session 并允许 handoff/exec 前的 `setpgid(child_pid, child_pid)`，shell wait4 时消费该身份并 handoff 到 grandchild，grandchild exit 后恢复 shell wait4 和 visible shell pid；不创建第二个 runnable `UserChild` task ref。最终 focused 1/1 复跑记录 `target=pending_child`，没有 `reason=pid_not_visible`，`TIOCSPGRP` 返回 0，`lost+found` 和 `user exit status=0` 保持成功。默认 `make test` 不纳入该 opt-in case；完整 task graph、通用普通 fork、完整 COW/mm、完整 wait/reap、post-exec parent setpgid 或 job-control signal 不在本片内。文档治理继续保留：`ROADMAP.md` 作为唯一优先级/状态索引，长历史细节逐步拆到 `docs/roadmap/*.md`。
12. LTP 首片取证已用两个旧 terminal identity 的真实 PTY 会话复现：native 与 linux-object 都在
    `run-syscalls.sh --list -- 'uname*'` 第 30 行首先触发 `unsupported syscall nr=59`，随后 BusyBox
    报 `can't create pipe: Function not implemented`。当前范围因此固定为正式 `ltp`/`ltp-lo`
    scripted acceptance 与单有界 `pipe2(flags=0)`/snapshot handoff，不提前处理 uname ELF/VFS、
    `personality(2)` 或其它尚未成为首个运行时边界的风险。实现后的两份正式 acceptance 已越过
    syscall 59，并一致停在同一行的 `clone(220) stage=child_context_unsupported`：`flags=0x11`、
    `newsp=0`、`current_child_continuation=1`，guest status 2，list 尚未输出 uname 条目；本轮按范围
    保留该新边界，不继续修改 clone 或后续路径。
13. 2026-07-19 对该 clone 边界补长期诊断后，根 `make test` 的 native/linux-object 正式 case 均记录
    `current_child_source=pid1_plain_fork`；外层 PID1 wait frame/address/fd/stack/writable snapshot 五类
    所有权均为 1，具体拒绝仅为旧实现的 `reject_outer_not_vfork=1`，总计 178/180、仍只两项 LTP
    红灯。下一片据此只增加单 builtin-only grandchild 的独立内层 continuation snapshot；不覆盖外层
    所有权、不允许 inner exec、不展开 COW/通用 task graph，并在 list 后首个新边界停止。
14. 同日二层 plain-fork 首片完成：独立内层 trap/frame/fs/fd/stack/writable snapshot 不覆盖外层 PID1
    所有权，空 pipe read 或 wait4 可有界 handoff 到 builtin-only grandchild，child exit 后只恢复 script
    parent 并保留 completed wait identity，后续 script exit 再恢复 PID1。native 与 linux-object 正式
    case 的 list 均各精确输出一次 `uname01/02/04` 后进入 `=== uname01: uname01`；首个新边界一致为
    builtin grandchild 的 `execve("/bin/sh", ["/bin/sh", "-c", "uname01"], envp)`，诊断为
    `exec_boundary=builtin_grandchild_enosys`。证据保存在
    `20260719T010657.225045Z-ltp-10960` 与 `20260719T011021.698067Z-ltp-lo-11145`；严格 3/3 判据未改，
    完整 inner exec、uname ELF/VFS/personality 与通用 pipe/task graph 继续 deferred。
15. inner-exec 对象 smoke 扩展曾稳定触发与目标逻辑无关的 kernel-init 栈溢出。HEAD 对照 5/5 通过，
    当前构建 3/3 在同一 regular-file open 边界失败；QEMU exception trace 的首个异常是
    `find_child` prologue 在栈底上方仅余 80 字节时发生 store page fault。逐 phase 帧反汇编将进入
    payload 前的 960 字节净差精确闭合到 `initcall::preset`：其长期帧因 `preset_objects` 内联从
    288 增至 1296 字节。把初始化临时对象隔离到会返回的 noinline helper 后，长期帧降至 320 字节，
    native/linux-object object smoke 均恢复 55/55；没有扩大内核栈或修改 VFS 行为。
16. builtin-grandchild inner exec 首片随后完成。第一次 exec 把 script parent 地址空间/`UserStack`
    绑定到 transaction retired 槽，第二次 exec 保留原 parent 并释放被替换 executable，CLOEXEC 只
    改 child fd view，grandchild exit 在 pipe-read 与 wait4 两条路径都先恢复原 SATP，script 最终退出
    再恢复 PID1。正式 native 证据为 `20260719T135137.750631Z-ltp-22256`，linux-object 为
    `20260719T135448.428722Z-ltp-lo-22518`：两侧 list 均各精确输出一次 `uname01/02/04`，旧
    `builtin_grandchild_enosys` 为 0；每个 uname 都记录 first retention、第二次 exec 释放 874 页及
    exit restore。新的首个稳定边界一致为 syscall 34 `mkdirat`，LTP 在 `tst_tmpdir.c:270` 的
    `mkdtemp(/tmp/LTP_unaXXXXXX)` 得到 ENOSYS/TBROK。严格 3/3 判据未改，两份 result 均为红灯且记录
    private disk removed、process group reaped；mkdirat 不在本片继续实现。
17. 默认 LTP 门禁随后按项目验收目标收缩为双 provider `close*` list-only：两侧只执行
    `run-syscalls.sh --list -- 'close*'`，要求 `close01/02` 各精确出现一次，并以 list 原始状态退出。
    不执行 `close01/02` binary，也不增加或声明 `close(57)` smoke 覆盖；原 uname 运行证据与
    `mkdirat(34)` 边界继续作为历史记录，但不再阻塞默认根回归。focused native 证据为
    `20260719T143245.923759Z-ltp-31338`，linux-object 为
    `20260719T143318.374010Z-ltp-lo-31437`；两侧均以 guest status 0 完成并清理私盘。

## 已完成里程碑

| 领域 | 结果 | 说明 |
| --- | --- | --- |
| arceos_ex | 独立对象级实验目录落地 | `impl/arceos_ex/` 已包含 Makefile、RISC-V64 linker script、入口汇编和 no-alloc Rust 源码骨架。 |
| arceos_ex | 对象级公共基础落地 | 已定义状态集合、transition 集合、`EventResult`、生命周期 transition 检查和 checkpoint hook。 |
| model/derive | 推导义务阶段性收口 | 当前 `make verify` 为 `0 obligation / 45 deferred`。 |
| arceos_ex | `BootInitFlow.Preset` 入口步骤当前轮复核完成 | 已按 Linux `head.S` / `setup_vm()` 对照复核入口步骤锁/同步面：该步骤无 mutex/spinlock/rwlock/RCU guard 临界区，保留系统独占、早期中断关闭、init task preempt disabled 和 RISC-V SATP/`sfence.vma` 地址转换同步事实；`apply_early_boot_alternatives()` 已作为 `Vm.Preset` 显式 deferred/ready 事实记录，实际 alternatives/errata text patch 对象后续再展开。 |
| arceos_ex | `BootInitFlow.Setup` 入口直接编排当前轮复核完成 | 已覆盖 EarlyDtb、PlatformCpuInfo、PhysicalMemory、InterruptType、BootCPU setup/enable、PrintkBuffer、CommandLine/KernelCmdline、EarlyParam、SBI、EarlyCon、MemBlock、InitMM、EarlyIoremap 和 SwapperVm；按 sibling `.config` 复核后，`init_vmlinux_build_id()`、`page_address_init()` 为空调用并记录为 trimmed；`phys_ram_base`、`kernel_map.va_pa_offset`、DMA32 zone input、hugetlb CMA reserve deferred 和 `CONFIG_STRICT_KERNEL_RWX` 最终权限拆分事实保持不变。 |
| arceos_ex | `CorePreparePhase` 当前轮复核完成 | 已按 Linux `setup_arch()` 后半段和 `start_kernel()` 中 `setup_arch()` 返回后到 `trap_init()` 对照复核锁/同步面；现有模型已覆盖 DeviceTree、Zones、PageMetadataMap、ResourceTree、CpuGroup.setup_smp、CacheBlockInfo、CpuCapabilities、DmaCachePolicy、CommandLine saved/static 视图、PerCpuStorage、CpuHotplugState、Params、BootParam、PayloadParam、Randomness、PrintkBuffer.setup、ExceptionTable 和 ExceptionType.setup，并用 `ResourceTreeWriteContext`、`CpuHotplugReadContext`、`StaticBranchJumpLabelContext`、`PrintkBufferSetupLocalInterruptContext` 保留 resource_lock、cpu_hotplug_lock、jump_label_mutex 和 local IRQ save/restore 协议；本轮补齐 `Randomness.Preset` 对 `random_init_early()` 主线不经 input-pool lock 与条件 base-crng irqsave deferred 的实现观测。 |
| arceos_ex | `MmCoreInitPhase` 最小闭环 | 已覆盖 MemoryTopology、MemoryNode/ZoneSet/ZonelistSet、PageAllocator、MemoryDebugHardening、StackDepot、Swiotlb、SlubSubsystem、KernelGlobalAllocator、DynamicContainerRuntime、PageTableCaches、VmallocAllocator 和 MmStructCache。 |
| arceos_ex | `SchedInitPhase` 最小闭环 | 已正式落到 `spec/model/boot/sched-init/` 和 `impl/arceos_ex/src/phases/boot/sched_init.rs`，覆盖 Scheduler、BootRunQueue、BootTask、RadixTree、MapleTree、Workqueue.Prepared、Softirq.Prepared、RcuCore 和 `Scheduler.schedule_preempt_disabled()` smoke。 |
| arceos_ex | `IrqTimeInitPhase` / `LocalIrqEnablePhase` 最小闭环 | `IrqTimeInitPhase` 已正式落到 `spec/model/interrupt/irq-time-init/` 和 `impl/arceos_ex/src/phases/interrupt/irq_time_init.rs`，覆盖 IrqController、IrqDispatchTree、Tick/TimerWheel/HrtimerCore、Timekeeper、RiscvTimerProvider、Softirq.Ready、Randomness.Ready、SbiIpi、SmpCallFunction，并保持 boot CPU 本地中断关闭；`LocalIrqEnablePhase` 独立落到 `spec/model/interrupt/local-irq-enable/` 和 `impl/arceos_ex/src/phases/interrupt/local_irq_enable.rs`，只打开 boot CPU 本地中断总入口，新增 timer interrupt smoke。 |
| arceos_ex | `IrqOpenPreparePhase` 当前轮复核完成 | 已按 Linux `start_kernel()` 中 `kmem_cache_init_late()` 到 `arch_cpu_finalize_init()` 对照补齐结构化 `IrqOpenPrepareTrimmedPaths`，记录 `panic_later`、lockdep/selftest、initrd/NUMA/ACPI/late_time/arch finalize 裁剪依据和 `setup_per_cpu_pageset()` deferred；`SchedClock.setup()` 已按当前 `../linux-6.12/.config` 的 RISC-V 配置 修正为 `local_irq_disable()` / `local_irq_enable()` guard 并由 smoke 观测。 |
| arceos_ex | `ProcessPreparePhase` 当前轮复核完成 | 已按 Linux `start_kernel()` 中 `pid_idr_init()` 到 `kcsan_init()` / `rest_init()` 前的区间复核锁/同步面；现有模型覆盖 RootPidNamespace、TaskCreationCore、CredentialCore、VMA/task context、namespace、keyring/security、VFS/ramfs 初始 rootfs 等 rest_init 输入事实，本轮补齐 `ProcessPrepareTrimmedPaths`，结构化记录 x86 EFI/SCS/lockdep/KGDB/cpuset/cgroup/taskstats/delayacct/ACPI/KCSAN 裁剪依据、pagecache/proc/nsfs/pidfs/net namespace/bdev/chrdev deferred，以及 `rcu_init_tasks_generic()` 属于 `kernel_init_freeable()` 的 out-of-scope 事实；实现侧新增对象、ready check、checkpoint 和 process_prepare smoke 观测。 |
| arceos_ex | rest_init owner-split 最小闭环 | 已正式落到 `spec/model/phases/boot-init/rest-init/` 和 `impl/arceos_ex/src/phases/boot_init/rest_init.rs`，覆盖 PID 1、kthreadd、SystemState.Scheduling、kthreadd_done、Scheduler 首次调度交接和 BootIdleRuntime，不再保留 `RestInitPhase` wrapper 对象身份。 |
| arceos_ex | `PreSmpInitPhase` 当前轮复核完成 | 已按 Linux `kernel_init()` / `kernel_init_freeable()` 中 `wait_for_completion(&kthreadd_done)` 到 `smp_init()` 前的区间复核锁/同步面；规格侧新增 `KernelInitKthreaddDoneWait` wait-side 边界，明确 `KthreaddReadyGate.complete()` 只发布 completion token/wake，PID 1 release fact 由 KernelInitTask wait side 观察后提交；`workqueue_init()` 已复用 `within WorkqueuePoolMutexContext { ... }` 表达 `wq_pool_mutex` guard，实现侧新增 KernelInitTask owner lock/unlock 观测。 |
| arceos_ex | `SmpBringupPhase` 当前轮复核完成 | 已按 Linux `smp_init()` 对照补齐 CPU hotplug 同步面；规格/实现保留 `cpus_read_lock()` + `smpboot_threads_lock`、`cpu_add_remove_lock` + `cpus_write_lock()`、`cpu_running` / `done_up` completion wait.lock irqsave、SBI boot-data publish barriers，以及 AP IPI/flush/local-IRQ/hotplug-thread memory-order summary facts。 |
| arceos_ex | `RuntimeCorePhase` 当前轮复核完成 | 已按 Linux 6.12 `kernel_init_freeable()` 中 `sched_init_smp()` 到 `page_alloc_init_late()` 对照补齐锁/同步面；规格/实现保留 `sched_domains_mutex`、`workqueue_init_topology()` 的 `wq_pool_mutex` + 聚合 `wq->mutex` guard、Async/Padata deferred 责任细分，以及 `CONFIG_DEFERRED_STRUCT_PAGE_INIT=n`、`CONFIG_PAGE_EXTENSION=n`、`CONFIG_SHUFFLE_PAGE_ALLOCATOR=n` 的 page allocator late 裁剪依据。 |
| arceos_ex | `InitcallPhase` 当前轮复核完成 | 已按 Linux 6.12 `init/main.c::do_basic_setup()`、`do_initcalls()` 和 `do_one_initcall()` 完成当前轮复核；当前 `.config` 下 `CONFIG_CPUSETS`/`CONFIG_CGROUPS` 和 `CONFIG_CONSTRUCTORS` 裁剪已结构化记录，`devices_init()`/`buses_init()`/`classes_init()`/`firmware_init()` 前置事实、`bus_register()` 的 `subsys_private.mutex`/klist 事实继续 formal，`devtmpfs_init()` 的 req_lock/completion/kthread、`of_core_init()` 的 `of_mutex`、`init_irq_proc()` 的 procfs/default_smp_affinity/effective_affinity 导出以及 driver_init 尾部 sysfs/system bus 调用保持显式 deferred。`do_initcalls()` 只固定 Linux level order，dispatcher 保持 entry-agnostic；同级 entry 顺序无关性暂不由 derivation 证明，已记录为 proof/nightly deferred；`do_one_initcall()` 的 blacklist/filter、trace boundary、preempt count 快照/修复-or-absent、disabled IRQ 修复-or-absent 和 latent entropy accounting 已作为表内 entry facts 暴露。本轮未新增 formal `within` context：`do_one_initcall()` 的 preempt/IRQ 修复边界是 per-entry action fact，不是词法 guard。验证：`make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、`make run`、`make run APP=user-boot`、`git diff --check` 均通过。 |
| arceos_ex | `RootfsPhase` 当前轮复核完成 | 已按 Linux 6.12 `kernel_init_freeable()` 中 `kunit_run_all_tests()` 到 `integrity_load_keys()` 对照补齐 rootfs 子阶段边界；规格/实现新增 `RootfsPrepareNamespacePaths`，结构化记录 `wait_for_device_probe()` 的 deferred_probe_work/probe_count/probe_waitqueue 同步责任、`CONFIG_MD=y` 下 `md_run_setup()` deferred、`CONFIG_BLK_DEV_INITRD=n` 下 initrd trimmed、cmdline absent 下 rootdelay/rootwait trimmed、NFS/nodev root deferred、CIFS root trimmed、Linux `CONFIG_EXT2_FS=n`/`CONFIG_EXT4_USE_FOR_EXT2=y` 与 arceos_ex `Ext2Driver` 替代关系，以及 `CONFIG_DEVTMPFS=y` 下 devtmpfs_mount deferred/当前 DevFs 不在 ext2 root 下 remount。`IntegrityKeysDeferred` 现记录 `CONFIG_INTEGRITY=y` 位置和 `CONFIG_IMA=n`/`CONFIG_EVM=n` 裁剪依据；本轮无新增 `within` context，RootFS enable 仍是正式 prepare_namespace 事件。验证：`make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke` 复跑通过、普通 `make run` 通过、普通 `make run APP=user-boot` 复跑通过、`git diff --check` 通过；首次 smoke/user-boot run 均命中已知 DF-0002 initcall 间歇失败。 |
| arceos_ex | `FinalizePhase` 当前轮复核完成 | 已按 Linux 6.12 `kernel_init()` 中 `kernel_init_freeable()` 返回后到 `do_sysctl_args()` 返回的区间复核锁/同步面；规格/实现保留 `async_synchronize_full()` 的 waitqueue、`async_lock` irqsave spinlock、`entry_count` atomic 和全局 cookie ordering deferred facts，补齐 `numa_default_policy()` 在 `SYSTEM_RUNNING` 之后的 `CONFIG_NUMA=n` trimmed 位置，并记录 `rcu_end_inkernel_boot()` 的 atomic decrement、RCU lazy trimmed、`rcu_normal_after_boot` WRITE_ONCE trimmed/recorded 和 `rcu_boot_ended` publish facts。 |
| arceos_ex | `PayloadPhase` 当前轮复核完成 | 已按 Linux 6.12 `init/main.c::kernel_init()` 中 `do_sysctl_args()` 返回后到首次成功 `run_init_process()` / `kernel_execve()` 不返回交接的区间复核；确认 `PayloadPhase` 是 `Kernel` 生命周期末尾阶段，不是 `SmpRuntimePhase` 子阶段，入口依赖 `FinalizePhase.Ready`、`FinalizeBoundary.Ready`、`SystemState.Online/SYSTEM_RUNNING` 和 `KernelInitTask.Online`。本轮新增 `PayloadExecSyncBoundaries` 结构化记录 Linux exec 同步面：`binfmt_lock`、`cred_guard_mutex`、`exec_update_lock`、`exec_mmap()` local IRQ disable/enable、`mmap_lock`、`sighand->siglock`、`tasklist_lock`、`fs->lock` + RCU read side、membarrier/switch-mm ordering、`bprm_mm_init()` / `finalize_exec()` task_lock rlimit 读取、`exec_mmap()` task_lock mm 交接、`sched_mm_cid_*()` rq_lock_irqsave + `smp_mb()`、files unshare / CLOEXEC `file_lock`、`io_uring_task_cancel()`、POSIX timer `siglock`、namespace switch 和 exec 成功后的 rseq/perf/audit/accounting hooks 均保持 deferred；完整 binfmt/script retry 保持 deferred，`CONFIG_MODULES=n` 下 `request_module("binfmt-...")` retry 记录为 trimmed/no-op；`CONFIG_BLK_DEV_INITRD=n` 下 ramdisk init 分支、`CONFIG_DEFAULT_INIT=""` 下 default init 分支记录为 trimmed/no-op。`UserAppRuntime.Action::EnterUserMode` 现用 `within UserModeTrapReturnContext { ... }` 表达最终 RISC-V trap-return 交接，要求 `satp` 写入后 `sfence.vma` 并通过 `sret` 不返回；实现侧新增 `PayloadExecSyncBoundaries` context 字段、payload smoke 观测和 trap-return context/sfence/sret 事实。已查 `docs/DEFECTS.md`：DF-0001 user-boot 间歇读 `/sbin/init` 属既有缺陷，本轮不重新归因。 |
| arceos_ex | no-alloc 输出路径 | 启动期内部输出前端和应用侧最小 `println!` 前端都写入 `PrintkBuffer`，再由 `EarlyCon(SBI)` drain。 |
| arceos_ex | 动态容器前置链路 | 已完成 `PageAllocator -> SLUB/kmalloc -> GlobalAlloc -> Vec` 首轮闭环，支持后续 OF platform device population 使用 `Vec` 保存 `PlatformDevice` 和 `DeviceRef`。 |
| arceos_ex | vmalloc runtime 映射能力扩展 | 已支持完整 `VMALLOC_START..VMALLOC_END` runtime vmalloc 范围，跨 L0/PTE window 映射、按需追加 L1/L0/PTE 页表页、稀疏 slot metadata，以及动态 `VmapArea` / `VmapMapping` 记录 backing；更完整的 Linux `vm_struct/vmap_area` 行为仍在进行中。 |
| arceos_ex | 最小 FDT 解析 | 不引入外部 crate，不使用 `Vec`、`String`、`Box`，只解析当前闭环必要节点。 |
| arceos_ex | 顶层 Makefile 入口 | 顶层 `make build`、`make run`、`make run PROBE=announce`、`make verify`、`make clean` 已可用，并支持 `APP=smoke` / `APP=hello` payload 选择；`LOG=trace` 暂时作为 `PROBE=announce` 兼容 alias 保留。 |
| arceos_ex | announce/checkpoint 一致性复查 | `make verify REPORT=graph`、`make run PROBE=announce` 和实现阶段顺序一致，checkpoint 单字符映射无重复。 |
| spec/coding | 规则强度分层 | 已为 `MUST`、`SHOULD`、`MAY`、`NOTE` 建立统一标注与解释规则，并把全局 `Context` 映射记录为 `SHOULD`。 |
| arceos_ex | 第一轮源码结构清理 | 已把入口 Setup、入口 Preset、FDT、静态页表、VM setup bridge 等聚合责任拆入相应私有模块，保留后续目录分层为 P2。 |

## 当前交接标记

- 当前 PLIC/IRQ 前置链路已完成首轮 `UartInterruptChainProbe` 验证：PlatformBus 已完成设备 population、driver register/probe、真实 ns16550a probe/bind、UART 8250 资源解析、ioremap/vmalloc UART MMIO 映射、console/earlycon handoff、PLIC irqchip init/provider setup、`PlicIrqDomain` source -> logical IRQ mapping、UART IRQ handler action 登记、root external input gate / UART source gate 显式 open，以及一轮真实 UART THRE interrupt cycle 下的 PLIC claim loop、IRQ dispatch、UART handler、PLIC complete、zero claim loop exit 和成对闭合观测。serial8250 console 已切到 interrupt-driven TX 当前轮：`printk` 经 TX queue + THRI 中断由真实 handler drain，已覆盖 burst、long、long-burst 和 quiesce/no-spurious-IRQ 检查；更完整 RX/TTY runtime、file/stdout/stdin 和用户态 I/O 后续单独推进。
- Linux PLIC object 复用专项已完成首轮收尾：`irq-sifive-plic.o` 由 `impl/providers/linux-6.12.mk` 声明，默认来自兄弟项目 `../linux-6.12/drivers/irqchip/irq-sifive-plic.o`，`arceos_ex` 默认 RISC-V ABI 已切到 soft-float，`PLIC_PROVIDER=linux-object` 会直接链接该对象并保留 Linux init/irqchip/alternative/bug/percpu sections。`objects/linux_plic_shim.rs` 已覆盖 `nm -u` 发现的 undefined symbols；`.initcall6.init` 已被执行，`__platform_driver_register` 已记录 `plic_driver`，最小 `platform_device/device/fwnode` 视图已触发 `plic_platform_probe()` 并完成 probe 到 domain/chained handler 注册。当前 Linux object provider 已通过 `plic_irqdomain_ops.alloc -> irq_domain_set_info -> handle_fasteoi_irq -> plic_chip.irq_eoi` 的 Linux-shaped leaf IRQ 主线承接 UART interrupt cycle，并通过显式 event 覆盖 `irq_enable/irq_disable/irq_mask/irq_unmask/disabled_irq_eoi/edge_irq_ack/unmapped_irq_fail` callback/service/fail boundary；native/linux-object provider 已收敛到同一薄 `objects::plic_provider` contract。checkpoint/KUnit handler 已收口为只读 observer；当前顶层 `make test` 覆盖 native 与 linux-object provider 矩阵。后续不再主动扩展 PLIC deferred 项，除非新目标实际踩到。
- 已解决前置问题：`make run APP=hello` 的重复输出来自 handoff 后 `PrintkBuffer` legacy drain cursor 未移交，以及 payload 末尾直接调用 earlycon backend drain；当前已修复为 boot pending flush、serial delivered cursor 推进、handoff 后 EarlyCon offline/panic guard，并要求 hello/smoke 只走 printk 前端。已通过 `make run APP=hello`、`make run PROBE=announce APP=hello` 与 `make test` 验证。
- `SmpRuntimePhase` 六个子阶段均已达到当前轮复核完成口径：`PreSmpInitPhase` 已补齐 KernelInitTask wait-side release 与 `workqueue_init()` pool mutex guard；`SmpBringupPhase` 已补齐 hotplug read/write guard、completion wait.lock irqsave、SBI boot-data ordering 和 AP local sync summary facts；`RuntimeCorePhase` 已补齐 `sched_domains_mutex`、workqueue topology mutex guard、Async/Padata deferred 责任和 page allocator late 裁剪依据；`InitcallPhase` 已完成 `do_basic_setup()`/`do_initcalls()` 复核并保留同级 initcall permutation proof/nightly deferred；`RootfsPhase` 已完成 `prepare_namespace()` / integrity keys 边界复核；`FinalizePhase` 已完成 `async_synchronize_full()` 到 `do_sysctl_args()` 区间复核。AP 内部 entry/callback 细节仍保持 deferred。
- 最近用户态 payload 与缺陷回归基线：`APP=user-boot` 已能运行默认 dynamic musl overlay `/sbin/init`，输出 `user hello` / `user exit status=0`；DF-0001 原始 `/sbin/init` 读取失败已经修复并归档。DF-0002 近期 `stress-mem` 回归中，普通 smoke 入口 500/500 成功，普通 user-boot 交叉入口 500/500 成功；后续仍保留普通 `make run APP=smoke`、普通 `make run APP=user-boot` 和 `stress-mem` 入口作为长期回归。
- 当前 trace SVG 产物：`tools/out/trace/main.trace.svg`。
- 当前 Ext2 最小 VFS read-only mount/path read 已完成：`Ext2Driver`、`Ext2Volume`、`Ext2FileSystem` 三对象边界已落地；`Ext2FileSystem.Enable` 通过 `VfsCore` 挂到 staging mount point，RootfsPhase 随后把该 mount 移到 `/` 并更新 `FsStruct.root/pwd`，VFS 负责当前 root 下的 lookup/open/read 入口，Ext2 后端继续经 `BufferHead` 读取真实 virtio-blk/ext2 镜像中的 Alpine rootfs 文件，并支持按目录 inode 解析多级路径组件、regular file 多 direct-block 和 single-indirect read。仍保持 read-only、no page cache、no write；目录 indirect、double/triple indirect、allocation 和写路径 deferred。
- 后续低优先级探索：对象封装为组件试验暂不作为 PLIC 首轮基线冻结后的默认下一阶段。恢复时先围绕组件封装试验明确对象选择、封装边界和验收方式，再进入实现。
- 后续低优先级清理：`LOG=trace` 只作为 `PROBE=announce` 的兼容 alias 保留。待所有文档、脚本、stress case 和常用命令迁移后删除该入口，把 `trace` 命名留给 Linux-like trace。
- nightly/压力测试补充方向：新增 initcall permutation 测试，扰动同一 level 内 initcall entry 顺序并比较 canonical final facts，用于覆盖当前 derivation 尚未证明的同级顺序无关性；该类测试纳入 nightly/manual 压力集合，不进入普通 smoke。
- 上下文清理后恢复：先执行 `git status --short --branch`；预期工作树干净。
