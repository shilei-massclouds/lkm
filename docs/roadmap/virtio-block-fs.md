# virtio / block / VFS / Ext2 历史

> 本文从 `docs/ROADMAP.md` 拆出，只保留专题历史、证据和细节。当前优先级、状态和下一步顺序以 [`../ROADMAP.md`](../ROADMAP.md) 为唯一入口。

## 下一阶段计划：virtio-rng 与 virtio 基础对象

本节是当前 P0 的执行清单。目标不是完整 virtio 子系统，而是用最简单的真实 virtio 设备 `virtio-rng` 推动可复用的 `virtio-mmio`、virtio core 和 split virtqueue 对象建模与实现。每完成一步必须先检查并确认执行结果，再进入下一步；检查结果应同步到本节状态，必要时同步 `spec/charter/main.md`、`spec/model` 和 `spec/coding/arceos_ex.md`。

### 已确认原则

1. `PlatformDevice` 和 `PlatformDriver` 保持为通用类型/机制边界；`Ns16550aPlatformDriver`、`VirtioMmioPlatformDriver`、`VirtioMmioPlatformDevice`、`VirtioMmioTransportDevice`、`VirtioDevice` 和 `VirtioRngDevice` 是具体实例对象或实例集合。
2. platform bus probe 观测分两层：第一层是通用 driver-core/platform-bus 事实，例如 driver registered、device discovered、match attempted、probe called、probe returned 和 device bound；第二层是具体对象事实，例如 driver/device ref 是否指向 `VirtioMmioPlatformDriver` / `VirtioMmioPlatformDevice`、compatible 是否落在预期集合、MMIO header 是否有效、virtio device id 是否为 `VIRTIO_ID_RNG`。
3. `virtio-mmio` 是普通 platform bus driver，首轮通过 `device_initcall` / platform driver register / OF match / probe 接入现有 `PlatformBus`，不要绕过 platform 总线直接扫描 FDT。
4. `virtio-rng` 首轮只作为真实 virtqueue 数据流闭合验证：提交一个 input buffer、notify、completion、`get_buf len > 0`。不接 hwrng 子系统、不接随机池、不暴露用户态接口，也不判断随机性质量。
5. split virtqueue 首轮只支持 single queue、direct descriptor、`add_inbuf`、`kick`、`get_buf`。packed ring、indirect descriptor、event idx、多队列、完整 DMA API/cache maintenance、reset/remove/suspend/resume 先显式 deferred。
6. 测试分层固定为：对象 API 和纯 fixture 构造走 smoke；真实 QEMU `virtio-rng-device` 链路走 checkpoint/KUnit 只读 observer。checkpoint/KUnit 不直接调用 driver/ring/IRQ action，不伪造 completion。smoke 可以有意构造条件，但不得要求普通对象暴露测试专用 API；历史 `fake_*` / completion injection 接口即使模拟真实外部事件，只要只服务 smoke/KUnit，也必须下沉到 smoke fixture/test harness。
7. 块 I/O 后续按 Linux 层次命名和建模：`BlockReadRequest` / `BlockIoBuffer` 不作为 Linux 对应主对象；真正面向后续 ext2 的对象应是 `Bio`、`BufferHead`、`submit_bio()` / `submit_bio_wait()` 和最小 `blk_mq_submit_bio()` 壳。当前 registry 同步读只是阶段性公开入口，不等同于 Linux 的完整 block layer。
8. 文件系统推进顺序先内存型、再设备命名、再块设备文件系统：先建立最小 VFS 对象和 `ramfs`，再建立 `devfs` 与 `rootfs`，最后回到 `bio` / `buffer_head` 并实现 read-only ext2。这样先丰富 smoke 可见文件系统行为，再让 ext2 接入已有 mount/root/dev namespace 和块 I/O 入口。

### 执行清单

1. **已完成首轮：泛化 platform bus 观测事实并保持 ns16550a 回归**。已新增通用 driver/device/match/probe/bind 观测事实和 `PlatformBus` 查询接口，smoke 同时覆盖 mock deferred 的通用 platform bus 行为与真实 ns16550a probe/bind 的通用观测结果；`platform_bus_ns16550a_*` 暂保留为 UART 专属回归锚点，不作为后续 virtio-mmio 的通用 bus 事实扩展方式。验收已通过：`git diff --check`、`make verify`、`make build APP=smoke`、`make run APP=smoke` 和 `make test`；diff 未扩大到 virtio 实现。
2. **已完成首轮：定义并实现 `VirtioMmioPlatformDriver` 与 `VirtioMmioPlatformDevice` 首轮模型**。已新增 `compatible = "virtio,mmio"` 的 platform driver 规格、静态 `device_initcall` 和 `VirtioMmioTransportDevice` 观测对象；`PlatformBus::probe_driver` 已按 Linux-like driver register 行为扫描全部匹配设备，而不是只绑定首个匹配设备。probe 当前从 `PlatformDevice -> DeviceNodeId -> DeviceTree node` 路径解析 `reg` 和 `interrupts`，通过 ioremap 建立 MMIO mapping，并读取 virtio-mmio magic/version/device_id/vendor_id；`device_id == 0` 记录为 placeholder/rejected，不注册 virtio core。验收已通过：`make verify` 为 `0 obligation`，`make build APP=smoke` 通过，`make run APP=smoke` 通过 37/37，`make test` summary 为 `overall total=87 pass=87 fail=0`；header classifier smoke 覆盖 valid/invalid magic、unsupported version、device_id 0 placeholder 和 device_id 4 rng candidate，默认 QEMU 路径通过 `virtio-rng-device` 观察到至少一个 `device_id == 4` 的 virtio-rng MMIO transport，其余 unused `virtio,mmio` slot 仍记录为 placeholder/rejected。
3. **已完成首轮：建立 virtio core 最小 bus/device 模型**。已新增 `VirtioBus` 和 `VirtioDevice` common 规格与 `Context` 下的全局 `VirtioBus` 实例；`VirtioBus` 与 `PlatformBus` 并列存在，`PlatformBus` 不拥有 virtio-specific 状态，public probe API 不出现 `VirtioBus` 业务参数。`virtio-mmio` probe 读到 valid、非 placeholder header 后，通过中性的 probe resources 调用 `VirtioBus` 正式注册 API，构造带 MMIO transport reference 的 generic `VirtioDevice`。已删除 `VIRTIO_MMIO_PROBE_SUMMARY`、`probe_summary()` 和 smoke 对临时 summary 的依赖；`virtio_mmio` app smoke 只保留纯 header classifier，真实 QEMU `virtio-rng-device` 路径由 `virtio-bus` checkpoint/KUnit 只读 observer 验证 `device_id == VIRTIO_ID_RNG`、MMIO transport 计数和 platform bus match/probe/bind 事实。验收已通过：`make verify`、`make build APP=smoke`、`make test-kunit` 和 `make run APP=smoke`。
4. **已完成首轮：建立 split `VirtioSplitRing` / `VirtQueue` 最小模型与实现**。已新增独立 `virtio_ring` 规格与实现；`VirtQueue` 是 driver-facing queue 对象，`VirtioSplitRing` 是当前 split-ring backing，支持 descriptor table、avail ring、used ring、free descriptor 管理、single inbuf 提交、`kick` 记录和 `get_buf`。ring backing 按 `queue_size` 建立，当前用 `Vec` 临时承载，不把 smoke 小队列固化成编译期固定数组；真实 DMA/coherent allocation 和 cache maintenance 仍 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke` 和 `make test`；smoke 通过 fixture 构造 used entry 条件，覆盖初始化、`add_inbuf`、descriptor exhaustion、completion consumer、empty completion、len 记录和 buffer ownership 边界。
5. **已完成首轮：实现 `VirtioRngDriver` / `VirtioRngDevice` 对象级 integration 闭环**。已新增独立 `virtio_rng` 规格与实现；`VirtioRngDriver` 通过 generic `VirtioDevice` 的 `device_id == VIRTIO_ID_RNG` 匹配，probe 后创建持有 single input `VirtQueue` 的 `VirtioRngDevice`。`request_entropy()` 提交 32/64 字节 input buffer 并 kick，smoke 通过 fixture 构造 used entry 条件，`complete_entropy()` 通过 `get_buf` 更新 `data_avail` / `data_idx` / request counters；`hwrng_register()`、随机池、文件系统、用户态 read、真实 MMIO notify/IRQ 和完整 reset/remove 仍 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`；smoke 覆盖 probe、request、completion `len > 0`、重复 request、len 0 和 cleanup 后拒绝。
6. **已完成当前轮：接入真实 QEMU `virtio-rng-device` checkpoint/KUnit 闭环**。真实路径已按规格经过 FDT `virtio,mmio` node、platform bus probe、MMIO header、virtio core match、`VirtioRngDriver` probe/common setup、single input split virtqueue setup、`add_inbuf`、MMIO `QueueNotify`、PLIC IRQ、virtio-mmio IRQ ack、used ring `get_buf` 和 `data_avail > 0`。queue setup 已按 MMIO `VERSION` 分流：QEMU virt 当前观测为 version 1，走 legacy `QUEUE_PFN` 路径；modern v2 desc/avail/used address 路径已保留实现边界。首轮使用页对齐 static coherent backing，通用 DMA/coherent allocator、cache maintenance、hwrng/random/user API 仍 deferred。验收已通过：`make verify`、`make test-kunit` 和 `make test`；KUnit `virtio_rng.real_completion` 在 `VirtioRng.EntropyReady` checkpoint 只读确认 `device_id == 4`、request/notify/irq/get_buf/completion 计数闭合、completion `len == 64`、`data_avail == 64` 且 data buffer 非全零；普通 `make run` / app smoke 不输出固定随机 payload。当前 `make test` summary 为 `overall total=53 pass=53 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `13/13`、app smoke `39/39`。
7. **已完成当前轮：建立 Linux-like `HwRngCore` / `HwRngDevice` 与 `virtrng_scan` / read 边界**。已先规格后实现 hwrng core registry/current-rng 模型，`VirtioRngDevice` 内嵌 `HwRngDevice` 和 `have_data: Completion` 子实例；`virtrng_probe()` 仍只对应 probe_common，`virtrng_scan()` 作为 driver callback 调用 `HwRngCore.register()`，注册后首个 `virtio_rng.0` 成为 `current_rng`。使用路径必须经 `HwRngCore.read_current()` / `HwRngDevice.read()` 转发到 `VirtioRngDevice.read_entropy()`，不允许 smoke 绕过 hwrng core 直接调用 virtio-rng 私有 read。首轮 smoke 覆盖 nonblocking 消费已完成数据、更新 `data_idx/data_avail`、耗尽后自动重新提交 entropy request；blocking wait、misc `/dev/hwrng`、sysfs selection/quality、random pool fill thread、freeze/restore 和完整 reset/remove 仍 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test-kunit` 和 `make test`；当前 `make test` summary 为 `overall total=53 pass=53 fail=0`。
8. **已完成当前轮：建立 virtqueue descriptor chain 规格与实现**。已对照 Linux `drivers/virtio/virtio_ring.c` 和 virtio split-ring 规格，把 `VirtQueue` 从 single direct input buffer 扩展到 descriptor chain：支持 out/in descriptor 混排、head token 管理、chain free/release、used len/status 消费和对象级错误边界。`virtio-blk` 首轮所需的三段 chain 能力已经作为通用 ring 能力落地；真实设备 completion 仍由后续 checkpoint/KUnit 只读观察。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke` 和 `make test`。
9. **已完成当前轮：抽象 virtio transport/config/features 通用边界**。已在 `virtio-mmio` queue setup/notify 基础上规格化可复用 transport 操作：status reset/ACK/DRIVER/DRIVER_OK、feature negotiation、config capacity read、queue select/ready/notify，以及 legacy v1 与 modern v2 差异边界；rng/blk 均通过 generic `VirtioDevice` 接口驱动 transport。完整 reset/remove/suspend/resume、多队列、DMA API/cache maintenance 和 packed ring 继续 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test-kunit` 和 `make test`。
10. **已完成当前轮：建立 `VirtioBlkDriver` / `VirtioBlkDevice` 发现/config KUnit 闭环**。已新增 `virtio_blk` 规格与实现；QEMU 默认参数新增 `virtio-blk-device`，`make disk` 自动生成 1MiB 全零 raw 镜像，`make run` 依赖该目标但 `make build` 不生成磁盘。`VirtioBus` 能同时注册 rng 和 blk；`VirtioBlkDriver` 匹配 `VIRTIO_ID_BLOCK`，probe 后读取 config capacity、建立单队列 split virtqueue 并置 DRIVER_OK。真实 QEMU 路径由 `virtio_bus` 与 `virtio_blk.discovery_config` checkpoint/KUnit 只读 observer 验证；本步不提交块读请求，不接 block layer、bio/request queue、page cache、文件系统、分区解析或用户态 `/dev` 接口。验收已通过：`make verify`、`make test-kunit`、`make build APP=smoke`、`make run APP=smoke`、`make test`；当前 `make test` summary 为 `overall total=54 pass=54 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `14/14`、app smoke `39/39`。
11. **已完成当前轮：实现最小 `VirtioBlkDriver` / `VirtioBlkDevice` read request 闭环**。已提交一个真实 read request：`virtio_blk_outhdr + data buffer + status byte` 三段 descriptor chain，MMIO notify 后由 IRQ completion 获取 used buffer 和 status OK。`make disk` 默认生成 8MiB raw 镜像并以 `FS_TYPE=ext2` 格式化；当前 read 读取 ext2 superblock 所在 sector 2，只验证数据非零与 ext2 magic，不引入 ext2/filesystem 对象或解析路径。首轮不建通用 block layer、bio/request queue、page cache、分区解析或用户态 `/dev` 接口；真实 QEMU `virtio-blk-device` 路径由 checkpoint/KUnit 只读观察，smoke 暂不承担本步验收。验收已通过：`make verify`、`make -C impl/arceos_ex disk`、`make test-kunit`、`make build APP=smoke` 和 `make test`；当前 `make test` summary 为 `overall total=55 pass=55 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `15/15`、app smoke `39/39`。
12. **已完成当前轮：建立 Linux-like block device registry / major-minor 发现路径**。已补 `BlockDevice` / `BlockDeviceRegistry` 规格对象和实现，`VirtioBlkDevice` 内嵌 `BlockDevice`，probe/setup 后通过全局 registry 发布为系统可发现块设备；registry 记录 `register_blkdev()`/`device_add_disk()` 形状事实、`devt`、默认设备、容量和 provider 关联，并支持 major/minor lookup。真实 QEMU `virtio-blk-device` 路径由 `virtio_blk.discovery_config` checkpoint/KUnit 只读验证注册事实和对象关联；更完整 request queue、bio/page cache、文件系统挂载、rootfs 和用户态 `/dev` 仍后续推进。
13. **已完成当前轮：通过 block registry/default 和 major-minor 公开路径发起真实读请求**。已先在 `block_device.spec` 建立 block-level read entry：`BlockDeviceRegistry.Action::ReadDefault` / `ReadByMajorMinor` 经 registry default 或 `devt` lookup 获得设备，再通过 `BlockDeviceProvider` 转发到 provider；`BlockDevice.Action::Read` 驱动 `VirtioBlkDevice.Action::ServeBlockRead`，要求 completion、拷贝到调用者 buffer、非零数据和读计数事实。实现侧新增 `BlockDeviceRegistry::read_default()` / `read_by_devt()` 和 `VirtioBlkLiveProvider`，smoke `block_device.read_default_and_devt` 只通过 registry 公开路径分别读取 ext2 superblock 所在 sector 2，验证 512 字节、非零数据和 ext2 magic，不绕过 registry 直接调用 `VirtioBlkDevice` 私有路径。为支撑 smoke 后续读，virtio-blk setup 会打开对应 virtio-mmio IRQ source gate，KUnit discovery 也只读验证该事实；`VirtioBlkReadReady` checkpoint 仍只在 initcall 首次读完成时触发，避免 smoke 二次读重复触发 KUnit handler。
14. **已完成当前轮：建立最小 VFS / ramfs / 初始 rootfs mount 规格与 smoke 闭环**。已建 `VfsCore`、`RamFsType`、`Mount`、`SuperBlock`、`Inode`、`Dentry`、`File` 等最小规格对象，明确 lookup/create/open/read/write/readdir/remove 的第一轮边界；在 `ProcessPreparePhase` 中对应 Linux `vfs_caches_init()->mnt_init()->init_mount_tree()` 建立 ramfs-backed 初始 rootfs mount。app smoke 不重新做 rootfs mount，而是在初始 rootfs 下创建挂载点，挂载第二个 ramfs，再基于该新 ramfs 覆盖目录创建、文件创建、写入、读回、lookup、readdir 和删除。该步不依赖块设备，也不引入 ext2、bio 或 page cache。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test` 和 `git diff --check`；当前 `make test` summary 为 `overall total=57 pass=57 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `15/15`、app smoke `41/41`。
15. **已完成当前轮：建立最小 devfs 并挂接 hwrng/block 设备对象**。已新增 `DevFs` 规格对象和实现，VFS 泛化出 `FileSystemKind::DevFs` 与 `VfsInodeKind::DeviceNode`；`InitcallPhase` 在 virtio-blk/virtio-rng live driver 完成注册后挂载 `/dev`，并创建 `hwrng` 与默认块设备节点。smoke 只验证 `/dev` 下 hwrng 和 block device 节点可发现，并检查它们绑定到 `HwRngCore.current_rng` 与 `BlockDeviceRegistry.default_device/devt`；本步不经 VFS file path 读写设备，device file ops、uevent、sysfs、权限模型、devtmpfs kernel thread 和用户态设备管理仍 deferred。
16. **已完成当前轮：rootfs enable / 真实根切换准备条件闭环**。`RootFS.Transition::Enable` 已从准备条件推进到 Linux-like `prepare_namespace()` 主线：记录 ramdisk eaccess 分支进入 `prepare_namespace()`，确认初始 ramfs rootfs 与 `FsStruct.root/pwd` 起点，确认 `DevFs` 已挂载、`BlockDeviceRegistry.default_device/devt` 可作为 root device candidate，并为后续 ext2 root mount/root switch 提供前置闭环。
17. **已完成当前轮：规格化 `Bio` / `submit_bio` / `BufferHead`**。已新增 `bio.spec`，建立 Linux-like `Bio`、同步 `submit_bio_wait()` / 最小 `blk_mq_submit_bio()` 壳，以及 `BufferHead` / `sb_bread()` / `__bread_gfp()` 路径；`BlockDeviceRegistry::read_default()` / `read_by_devt()` 保留为 `submit_bio_wait()` 下面的同步 adapter，负责 default 或 `devt` lookup 与 provider dispatch，不再作为高层 smoke 直接入口。实现侧新增 `objects::bio`，`BufferHead` 保留 512 字节 sector 入口，并新增 1KiB filesystem block 读取入口供 ext2 首轮使用；app smoke `block_io.buffer_head_read_default_and_devt` 经 `sb_bread_default()` 和 `sb_bread_by_devt()` 在真实 virtio-blk 上读取 ext2 superblock sector 2，验证 uptodate、submit/completion、非零数据和 ext2 magic。完整 tag set、多硬件队列、scheduler、page cache、async bio、writeback 和 4KiB filesystem block 泛化仍 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke` 和 `make test`。
18. **已完成当前轮：实现 read-only ext2 首轮路径**。首轮当时以 `Ext2Type` / `Ext2Mount` 建立规格和 coding 约束；当前它们已经被 `Ext2Driver` / `Ext2Volume` / `Ext2FileSystem` 三对象模型取代。实现侧通过 `BufferHead` 支持 1KiB filesystem block 读取，按 ext2 传统路径读取 superblock、group descriptor、root inode、root directory 和一个固定普通文件；`make disk` 在 1KiB block ext2 镜像中写入固定 `smoke.txt`，smoke 显式只读挂载默认块设备、lookup root dir 并通过 direct block 读回固定内容。本轮不经 VFS/rootfs file path，不实现 page cache/folio、间接块、写路径、权限、xattr、quota、remount 或错误恢复。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test` 和 `git diff --check`；当前 `make test` summary 为 `overall total=59 pass=59 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `15/15`、app smoke `43/43`。
19. **已完成当前轮：支持 4K Buffer / 4K ext2 block**。已把 1KiB 首轮约束泛化为 `BufferHead` 和 virtio-blk read buffer 至少支持 4KiB；当前 `Ext2Volume` / `Ext2FileSystem` 支持 ext2 `block_size` 1KiB/2KiB/4KiB，正确处理 4KiB 文件系统中 superblock 位于 byte offset 1024、group descriptor 起始 block 与 inode table block 的差异；`make disk` 不再强制 `mkfs.ext2 -b 1024`，回到默认 ext2 block size，并支持显式 `EXT2_BLOCK_SIZE` 参数。默认 smoke 镜像已确认为 4KiB block，并通过固定 `smoke.txt` direct-block read。验收已通过：`make verify`、`make build APP=smoke`、连续两次 `make run APP=smoke`、`make test`；当前 `make test` summary 为 `overall total=59 pass=59 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `15/15`、app smoke `43/43`。
20. **已完成当前轮：Ext2 对象建模讨论与规格/实现收敛**。已在 model/coding 层把旧 `Ext2Type` / `Ext2Mount` 边界拆分为 `Ext2Driver`、`Ext2Volume` 和 `Ext2FileSystem`：`Ext2Driver` 对应 Linux-like file system driver 和操作集，`Ext2Volume` 对应块设备上的 ext2 布局/元数据存在性检查，`Ext2FileSystem` 对应 mount 后的内存中文件系统实例。`Ext2FileSystem::Preset` 依赖 driver 与 volume，`Setup` 基于 volume 展开 superblock、group descriptor、root inode/root dentry 等结构化事实，`Enable` 只记录挂到上级 VFS 目录的生命周期边界，真实 VFS mount 集成仍 deferred；absence/invalid ext2 volume 是普通非致命错误，不导致内核崩溃或终止。smoke 已覆盖 driver setup、volume preset、filesystem preset/setup/enable、root lookup 和固定文件 direct-block read。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test`；当前 `make test` summary 为 `overall total=59 pass=59 fail=0`，其中 spec verify `1/1`、KUnit checkpoints `15/15`、app smoke `43/43`。
21. **已完成当前轮：Ext2 最小 VFS read-only mount/read**。已把 `Ext2FileSystem.Enable` 从 deferred mount boundary 推进为显式 `VfsCore` read-only mount：VFS 承载 mount point、Ext2 superblock/private、read-only dentry/inode、lookup/open/read 入口，实际 lookup/read 后端仍调度到 Ext2 的 `BufferHead` + direct-block 路径；smoke 通过 VFS lookup/open/read 读取跨 block regular file。page cache、indirect block 和写路径继续 deferred。验收已通过：`make verify`、`make build APP=smoke`、`make run APP=smoke`、`make test` 和 `git diff --check`。
22. **已完成当前轮：VFS 最小 pathname walk / path read 与 rootfs 切换**。`VfsCore` 的 path API 已改为从 `FsStruct.root` 解析绝对路径；`RootFS.Transition::Enable` 在 `/root` staging mount 后分开执行 `VfsCore.Action::MoveMountToRoot` 与 `FsStruct.Action::ChrootDot`，使当前 root/pwd 指向 ext2 root。rootfs smoke 直接读取 `/etc/alpine-release`，ext2 smoke 直接读取 Alpine rootfs 中的 apk installed 数据库；`/dev` 不在本轮偷偷 remount 到新 root 下。
23. **已完成当前轮：改进磁盘 rootfs 构造方式**。`make disk` 默认只在 `VIRTIO_BLK_IMAGE` 不存在时创建镜像；`ROOTFS_URL` 指向 Alpine 3.24.1 riscv64 minirootfs，`ROOTFS_TARBALL` 缓存在 `build/rootfs-cache/` 下，缓存存在时不重复下载；ext2 镜像从解包后的真实 minirootfs 构造，不再写入 smoke 专用文件或 filler entries。为适配真实目录树，VFS/ext2 lookup 已补强为按目录 inode 解析多级路径，read path 按 VFS inode 绑定的 ext2 ino 读取。
24. **后续计划：Ext2 VFS inode/dentry cache 边界收敛**。当前 Ext2 inode/dirent 仍主要是 `Ext2FileSystem` 下的结构化记录，VFS 只绑定最小 read-only inode facts。后续应对齐 Linux `iget` / dentry cache 形状，明确 repeated lookup、negative lookup、inode identity、refcount/evict deferred 边界，再决定是否把 Ext2 inode record 提升为更独立的对象。
25. **后续计划：RootfsPhase root switch 后的命名空间补强**。在当前 ext2 root 切换闭环后，补齐 rootfs/devfs 的后续语义：决定 `/dev` 是否以及如何在新的 ext2 root 下重新挂接，补 mount namespace/chroot/cwd/pwd 的更多任务上下文规则，并为后续用户态 payload 路径提供稳定根目录视图。
26. **后续计划：page cache / address_space / folio read path**。当前 ext2 read 仍直接经 `BufferHead` 读 direct blocks。后续在规格层引入 Linux-like `AddressSpace`、page cache/folio 和 readpage/readahead 边界，让 VFS read 逐步从直接 block copy 迁移到 page cache 语义；writeback 和 mmap 仍后置。
27. **已完成当前轮：Ext2 single-indirect regular file read 支持**。为读取 Alpine rootfs 中的 musl loader，read-only Ext2/VFS/BufferHead 路径已支持 regular file 单级 indirect block read；目录 lookup 仍保持 direct-block 边界。double/triple indirect、allocation、写路径和 page cache 继续 deferred；后续若需要更大文件或发行版程序路径，再按正式对象边界补强。
