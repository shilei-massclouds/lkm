# 启动阶段审计历史

> 本文从 `docs/ROADMAP.md` 拆出，只保留专题历史、证据和细节。当前优先级、状态和下一步顺序以 [`../ROADMAP.md`](../ROADMAP.md) 为唯一入口。

> 下文“当前”“后续计划”“P0/P1”等措辞是当时执行快照，不构成活跃计划。

## 当前审计计划：impl 与 Linux 6.12 对照

后续回顾检查以当前项目 `impl/` 下由规格驱动生成的代码为对象，以 `../linux-6.12/` 为 Linux 参考源码。审计顺序按 formal model 的阶段树推进，先从 `Kernel` 生命周期下的 `BootPhase` 开始，再进入后续阶段。

审计发现问题时，默认先归类并修订规格，而不是直接修改 `impl/` 代码：

1. `model` 规格缺口：Linux 路径中的对象、阶段、状态、事件、依赖、事实或显式 deferred 在 `spec/model` 中缺失或边界不清。
2. `coding` 规格缺口：模型语义已有，但对象到 Rust 实现、目录落点、函数边界、checkpoint/KUnit/smoke 观测或 Linux-like 数据结构约束在 `spec/coding` 中不足。
3. 刻意偏离但未记录：当前实现有意简化或偏离 Linux，但未在 model/coding/roadmap 中记录差异分类、理由和 deferred 边界。
4. 实现偏离已有规格：规格已经明确，`impl/` 代码未遵守；此类问题先记录为生成约束或实现缺口，后续再按规格修正代码。

每个审计项应尽量形成可追踪的 cross-reference：Linux 源码路径、当前 `impl/` 路径、对应 `spec/model` 条目、对应 `spec/coding` 条目、差异分类，以及是否需要新增/修订 checkpoint、KUnit 或 smoke 观测。除非用户明确要求修实现，审计阶段的修改优先落到 `spec/model/**`、`spec/coding/**`、必要说明文档和本 roadmap。

启动阶段逐项审计时，并发/同步控制必须作为固定检查面覆盖：本地中断开关、抢占开关、自旋锁、Mutex、读写锁、RCU、CPU bring-up 同步量、内存/地址转换同步和 TLB/cache flush 边界都要明确判断是否已由 `spec/model` 表达；若 model 已表达但实现映射不清，再修订 `spec/coding` 或生成约束。`impl/` 只作为生成结果和 Linux 对照样本，发现偏差时默认先修规格。

启动子阶段的完成口径统一按 rest_init owner-split 路径当前做法执行：先对照 Linux 6.12 明确子阶段调用边界和配置裁剪，重点列出锁原语、同步原语、上下文边界、内存顺序和 deferred/trimmed 项；再补齐 `spec/model` 与 `spec/coding`；最后按规格补齐 `impl/arceos_ex`、checkpoint/KUnit/smoke 观测，并执行完整验证。已有完成项保留其当前状态；只有发现某阶段实际只做了最小闭环或局部补强时，才重新标记为待补审核。审核过程中的分组只作为分析结构，不能直接升级为正式 phase/subphase；新增或细分正式子阶段前必须先取得用户确认。

当前新增记录缺口：除已经按当前轮口径记录完成的 `SchedInitPhase` 和 rest_init owner-split 路径外，从迁移前 `EntryPreludePhase`（现 `BootInitFlow.Preset` 入口步骤）到 `ProcessPreparePhase` 仍有 8 个启动边界的 roadmap 状态多处停留在“最小闭环”或“已完成首轮”。这些阶段历史提交中可能已经做过 Linux 对照和锁/同步补强，但缺少按上述 rest_init owner-split 口径写明的完成记录。处理方式不是直接重做，也不先判定未完成；必须先按阶段树从前往后复核证据，再决定是只补 roadmap 完成记录，还是补 `spec/model`、`spec/coding`、`impl/arceos_ex` 与观测。

这 8 个边界的锁/同步复核记录回填顺序固定为：迁移前 `EntryPreludePhase`（现 `BootInitFlow.Preset` 入口步骤）、`EntrySuccessorPhase`、`CorePreparePhase`、`MmCoreInitPhase`、`IrqTimeInitPhase`、`LocalIrqEnablePhase`、`IrqOpenPreparePhase`、`ProcessPreparePhase`。`SchedInitPhase` 和 rest_init owner-split 路径只作为已完成当前轮的参照，不列入待复核队列。每个边界先确认既有提交、规格和实现是否已覆盖锁/同步/上下文/内存顺序；确认结果必须明确 Linux 源码路径、当前配置裁剪、涉及的锁/同步原语、是否使用 `within` 表达上下文、实现观测和验证命令。

当前进度：第 1 项迁移前 `EntryPreludePhase`（现 `BootInitFlow.Preset` 入口步骤）已按当前轮口径完成复核和补齐。Linux 对照路径为 `arch/riscv/kernel/head.S` 的 `_start_kernel` / `relocate_enable_mmu` / `.Lsetup_trap_vector` 以及 `arch/riscv/mm/init.c::setup_vm()`；当前 `.config` 为 `CONFIG_MMU=y`、`CONFIG_RISCV_ALTERNATIVE_EARLY=y`、`CONFIG_FPU=y`、`CONFIG_RISCV_ISA_V=y`、`CONFIG_RISCV_BOOT_SPINWAIT=n`。复核结论：该入口步骤没有 Linux mutex/spinlock/rwlock/RCU guard 临界区，formal `within` 不需要新增；并发边界由 `context_is(SystemExclusive)`、`interrupt_concurrency_closed()`、`task_concurrency_closed()`、`BootCpuLocalInterrupt.disabled()` 和 init task preempt disabled 事实承载。同步重点是 RISC-V 地址转换顺序：写 trampoline SATP 前的 `sfence.vma` 已由 `trampoline_vm_translation_sync_ready_before_satp(TrampolineVm)` 表达，写 early SATP 后的 `sfence.vma` 已由 `early_vm_translation_sync_complete(EarlyVm)` 表达，实现映射在 `impl/arceos_ex/src/arch/riscv64/csr.rs::arceos_ex_switch_to_early_vm`。本轮补齐了 `apply_early_boot_alternatives()` 的显式 deferred：`Vm.Preset` 记录 `riscv_early_boot_alternatives_deferred(Vm)` 和 `riscv_early_boot_alternatives_mmu_off_boundary_preserved(Vm)`，`spec/coding` 要求实现不得把该 Linux MMU-off text patch 边界默认为 absent/implemented，`impl/arceos_ex` 的 `Vm::entry_prelude_ready()` 已检查该 deferred 事实。当前结构未新增 formal 子阶段；spinwait secondary lottery 路径因当前配置未启用而不属于 BP main path。

当前进度：第 2 项 `EntrySuccessorPhase` 已按当前轮口径完成复核和补齐。Linux 对照路径为 `init/main.c::start_kernel()` 中 `init_vmlinux_build_id()`、`local_irq_disable()`、`boot_cpu_init()`、`page_address_init()`、`setup_arch()` 前后，以及 `arch/riscv/mm/init.c::setup_bootmem()` / `paging_init()` / `setup_vm_final()`；当前 `.config` 为 `CONFIG_64BIT=y`、`CONFIG_MMU=y`、`CONFIG_ZONE_DMA32=y`、`CONFIG_HUGETLB_PAGE=y`、`CONFIG_STRICT_KERNEL_RWX=y`、`CONFIG_EFI=y`。复核结论：本阶段除已在 `PrintkBufferSetupLocalInterruptContext` 表达的 `setup_log_buf()` 局部 `local_irq_save()/restore()` 外，没有新增 Linux mutex/spinlock/rwlock/RCU guard 临界区；formal `within` 不新增，系统独占、本地中断关闭、任务/SMP 并发关闭仍由阶段上下文事实承载。同步重点是完整内核页表切换：`setup_vm_final()` 后写 SATP 和 `sfence.vma` 已由 `swapper_vm_translation_sync_complete(SwapperVm)` 和实现 `SwapperVm::enable()` 表达。本轮补齐了 `init_vmlinux_build_id()`、`page_address_init()` 的显式 deferred/position-preserved 事实，`MemBlock.Setup` 的 `phys_ram_base`、`kernel_map.va_pa_offset`、DMA32 zone input 和 hugetlb CMA deferred 事实，以及 `SwapperVm.Setup` 的 `CONFIG_STRICT_KERNEL_RWX` 最终权限拆分 deferred 事实；实现侧由 `entry_successor_phase_ready()` 和 memblock smoke 观测这些事实。未新增 formal 子阶段；下一项进入 `CorePreparePhase` 复核。

当前进度：第 3 项 `CorePreparePhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核。Linux 对照路径为 `arch/riscv/kernel/setup.c::setup_arch()` 中 `paging_init()` 返回后的后半段，以及 `init/main.c::start_kernel()` 中 `setup_arch()` 返回后到 `trap_init()` 完成前的通用初始化；当前 `.config` 为 `CONFIG_MMU=y`、`CONFIG_SMP=y`、`CONFIG_JUMP_LABEL=y`、`CONFIG_FLATMEM=y`、`CONFIG_ZONE_DMA32=y`、`CONFIG_RISCV_DMA_NONCOHERENT=y`、`CONFIG_RISCV_ISA_ZICBOM=y`、`CONFIG_RISCV_ISA_ZICBOZ=y`、`CONFIG_ACPI=n`、`CONFIG_NUMA=n`、`CONFIG_KASAN=n`、`CONFIG_BOOT_CONFIG=n`。复核结论：阶段边界仍处于 `SystemExclusive`，本地中断关闭、任务并发关闭和 SMP 并发关闭由 `CorePreparePhase.Ready` invariant 与 `core_prepare_phase_ready()` 观测；已经需要保留的同步原语均已进入 formal `within`，包括 `ResourceTreeWriteContext` 对 `resource_lock.write_lock()/write_unlock()`、`CpuHotplugReadContext` 对 `cpus_read_lock()/cpus_read_unlock()`、`StaticBranchJumpLabelContext` 对 `jump_label_mutex`、以及 `PrintkBufferSetupLocalInterruptContext` 对 `local_irq_save()/local_irq_restore()`。`Randomness.Preset` 已明确主线不使用 `input_pool.lock`，条件 `base_crng.lock` irqsave 路径保持 deferred；本轮发现实现侧只在后续 `Randomness.setup()` 观测 deferred lock facts，已补齐 `Randomness::preset()` 的 early no-input-pool-lock、conditional-reseed-deferred 和 base-crng-lock-deferred facts，并纳入 `core_prepare_phase_ready()`。`StaticBranch.setup()` 的运行期 text patch / icache sync、`apply_boot_alternatives()`、`local_flush_tlb_kernel_range(VMEMMAP_START, VMEMMAP_END)` 和 ACPI/NUMA/KASAN/bootconfig/VFS/security 等路径均已按 formal deferred、trimmed 或 implementation checkpoint 分类。实现侧 `impl/arceos_ex/src/phases/boot/core_prepare.rs` 按规格顺序推进 `DeviceTree`、`Zones`、`PageMetadataMap`、`ResourceTree`、`CpuGroup`、`CacheBlockInfo`、`CpuCapabilities`、`DmaCachePolicy`、`PerCpuStorage`、`CpuHotplugState`、`Params`、`Randomness`、`PrintkBuffer`、`ExceptionTable` 和 `ExceptionType`，并检查 `ResourceLock`、`CpuHotplugLock`、`JumpLabelMutex`、`BootCpuLocalInterrupt` 与 `Randomness.Preset` early facts；smoke 覆盖 `device_tree`、`zones`、`resource_tree`、`cpu_group`、`cache_block_info`、`cpu_capabilities`、`per_cpu`、`params` 以及 `rwlock`、`mutex`、`percpu_rw_semaphore` 通用原语。未新增 formal 子阶段；下一项进入 `MmCoreInitPhase` 复核。

当前进度：第 4 项 `MmCoreInitPhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核。Linux 对照边界固定为 `init/main.c::start_kernel()` 中 `trap_init()` 返回后调用 `mm_core_init()`，到 `mm/mm_init.c::mm_core_init()` 返回前；关键调用顺序为 `build_all_zonelists(NULL)`、`page_alloc_init_cpuhp()`、`page_ext_init_flatmem()`、`mem_debugging_and_hardening_init()`、`kfence_alloc_pool_and_metadata()`、`report_meminit()`、`kmsan_init_shadow()`、`stack_depot_early_init()`、`mem_init()`、`kmem_cache_init()`、`page_ext_init_flatmem_late()`、`kmemleak_init()`、`ptlock_cache_init()`、`pgtable_cache_init()`、`debug_objects_mem_init()`、`vmalloc_init()`、条件 `page_ext_init()`、RISC-V 不适用的 `init_espfix_bsp()` / `pti_init()`、`kmsan_init_runtime()`、`mm_cache_init()` 和 `execmem_init()`。当前 `.config` 为 `CONFIG_MMU=y`、`CONFIG_SMP=y`、`CONFIG_FLATMEM=y`、`CONFIG_ZONE_DMA32=y`、`CONFIG_RISCV_DMA_NONCOHERENT=y`、`CONFIG_RISCV_ISA_ZICBOM=y`、`CONFIG_RISCV_ISA_ZICBOZ=y`、`CONFIG_SLUB=y`、`CONFIG_SWIOTLB=y`、`CONFIG_STACKDEPOT=y`、`CONFIG_STACKDEPOT_ALWAYS_INIT=n`、`CONFIG_PAGE_EXTENSION=n`、`CONFIG_KFENCE=n`、`CONFIG_KMSAN=n`、`CONFIG_DEBUG_KMEMLEAK=n`、`CONFIG_DEBUG_OBJECTS=n`、`CONFIG_EXECMEM` 未选中。复核结论：阶段边界仍处于 `SystemExclusive`，本地中断关闭、任务并发关闭和 SMP 并发关闭由 `MmCoreInitPhase.Ready` invariant 与 `mm_core_init_phase_ready()` 观测；`BootPhaseContext` 只作为上下文事实，不能消除 Linux 可见同步协议。已正式建模并复用 `within ZonelistPrintkDeferredContext { within ZonelistUpdateSeqWriteContext { ... } }`，对应 Linux `mm/page_alloc.c::__build_all_zonelists()` 中 `printk_deferred_enter()/exit()` 与 `write_seqlock_irqsave(&zonelist_update_seq, flags)` / `write_sequnlock_irqrestore()`；实现侧 `PageAllocator::preset()` 记录 enter/exit、irqsave/irqrestore 计数并由 page allocator smoke 覆盖。CPU hotplug 方面只注册 `CPUHP_PAGE_ALLOC` 与 `CPUHP_SLUB_DEAD` step，secondary CPU bring-up 和 AP/BP 同步量不在本阶段执行；SLUB bootstrap 明确处于 `slab_state=UP/Ready` 前，Linux `kmem_cache_init()` 此处不需要 `slab_mutex`，但 runtime per-cpu/node/global locks、slab mutex/FULL 生命周期和 debug/hardening 同步保持 deferred。SWIOTLB static pool area lock、StackDepot no-early-table、PageAllocator buddy API、SLUB/kmalloc API、GlobalAlloc、PageTableCaches、VmallocAllocator、Ioremap 和 MmStructCache 均已由 model/coding/impl ready facts 对齐；`vmalloc_init()` 的 vmap node / vmap block queue / vfree deferred locks、runtime vmap guard、RCU/lazy purge、远端 vmalloc shootdown 和 TLB/cache flush 语义以 guard contract 或 deferred fact 保留，当前 RISC-V 映射可见性通过本地 `sfence.vma` 观测。trimmed/no-op 路径已由 `MmCoreTrimmedPaths` 结构化记录：`PageExt`、`KFENCE`、`KMSAN`、`Kmemleak`、`DebugObjectsMemory`、RISC-V 不适用的 x86 `espfix`/`pti` 以及 `include/linux/execmem.h` 下 `CONFIG_EXECMEM` 未选中导致的 `execmem_init()` inline no-op。未新增 formal 子阶段；未发现需要补 `spec/model`、`spec/coding` 或 `impl/arceos_ex` 的新缺口。本轮为 roadmap 收口，验证命令为 `git diff --check`；下一项进入 `IrqTimeInitPhase` 复核。

当前进度：第 5 项 `IrqTimeInitPhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核和补齐。Linux 对照边界固定为 `init/main.c::start_kernel()` 中 `early_irq_init()` 到 `WARN(!irqs_disabled())`，不包含随后的 `early_boot_irqs_disabled = false` / `local_irq_enable()`；关键调用顺序为 `early_irq_init()`、`init_IRQ()`、`tick_init()`、`rcu_init_nohz()`、`init_timers()`、`srcu_init()`、`hrtimers_init()`、`softirq_init()`、`timekeeping_init()`、`time_init()`、`random_init()`、`kfence_init()`、`boot_init_stack_canary()`、`perf_event_init()`、`profile_init()` 和 `call_function_init()`。RISC-V 细节对照 `arch/riscv/kernel/irq.c::init_IRQ()`，其中 `init_irq_scs()` 因 `CONFIG_SHADOW_CALL_STACK=n` 裁剪为空，`CONFIG_IRQ_STACKS=y` / `CONFIG_VMAP_STACK=y` 要求记录 per-CPU vmap IRQ stack pointer 初始化；`sbi_ipi_init()` 位于 `irqchip_init()` 之后。时间路径对照 `kernel/time/timekeeping.c::timekeeping_init()`，必须保留 `raw_spin_lock_irqsave(&timekeeper_lock, flags)`、`write_seqcount_begin/end(&tk_core.seq)` 和 `raw_spin_unlock_irqrestore()` 的组合。当前 `.config` 还启用 `CONFIG_IRQ_DOMAIN=y`、`CONFIG_IRQ_DOMAIN_HIERARCHY=y`、`CONFIG_SPARSE_IRQ=y`、`CONFIG_GENERIC_IRQ_IPI_MUX=y`、`CONFIG_GENERIC_CLOCKEVENTS=y`、`CONFIG_GENERIC_CLOCKEVENTS_BROADCAST=y`、`CONFIG_NO_HZ_IDLE=y`、`CONFIG_HIGH_RES_TIMERS=y`、`CONFIG_PREEMPT_RCU=y`、`CONFIG_TREE_SRCU=y`、`CONFIG_PERF_EVENTS=y`、`CONFIG_PROFILING=y`、`CONFIG_RISCV_SBI=y`、`CONFIG_RISCV_TIMER=y`、`CONFIG_SIFIVE_PLIC=y`，`CONFIG_RCU_NOCB_CPU=n` 使 `rcu_init_nohz()` 在本调用点为 inline no-op，`CONFIG_KFENCE=n` 使 `kfence_init()` 为裁剪路径。复核结论：本阶段仍保持本地中断关闭、抢占关闭、任务并发关闭和 SMP 并发关闭；新增 formal `RiscvIrqStackInitContext`，用 `within RiscvIrqStackInitContext { ... }` 表达 `init_irq_stacks()` 的启动期 IRQ stack 初始化边界；复用并补强 `TimekeeperSeqWriteContext`，明确其对应 raw spinlock irqsave 与 seqcount writer 的词法 guard。`TimerBaseInitContext`、`HrtimerBaseInitContext`、`SrcuBootListDrainContext`、`BootStackCanaryInitContext`、`PerfPmusSrcuInitContext`、`SmpCallFunctionInitContext`、`PlicIrqDomainMappingContext` 和 `IrqControllerDescInitContext` 继续作为本阶段既有 `within` guard；softirq 自身的运行期执行、周期 tick enable、IPI runtime、RCU GP kthread、完整 irqdesc allocator、perf event runtime、profile buffer、UART/TTY runtime 和 hardirq entry `call_on_irq_stack()` 均保持 deferred。规格侧新增 `RiscvIrqStackSet` 与 `IrqTimeTrimmedPaths`，记录 IRQ stack/SCS、`rcu_init_nohz()` 和 `kfence_init()` 裁剪事实；coding 侧新增对应 mandatory requirement；实现侧在 `impl/arceos_ex` 补齐对象、phase drives、ready check、checkpoint 和 smoke 观测，并修正 `Softirq.Transition::Setup` 到 `HrtimerCore` 之后、`Timekeeper` 之前以匹配 Linux 顺序。未新增 formal 子阶段；验证命令为 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`；本轮普通 `APP=user-boot` 输出 `user hello` / `user exit status=0`。下一项进入 `LocalIrqEnablePhase` 复核。

当前进度：第 6 项 `LocalIrqEnablePhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核和补齐。Linux 对照边界固定为 `init/main.c::start_kernel()` 中 `WARN(!irqs_disabled())` 之后的 `early_boot_irqs_disabled = false; local_irq_enable();`，RISC-V 实现对照 `arch/riscv/include/asm/irqflags.h::arch_local_irq_enable()` 设置 `CSR_STATUS/SR_IE`；本阶段不包含后续 `kmem_cache_init_late()`。复核结论：`LocalIrqEnablePhase` 是独立操作，不包在 `within` 上下文中，因为该 transition 自身把 boot CPU local interrupt context 从 disabled 切到 enabled；实现必须先清 `early_boot_irqs_disabled`，再打开 `sstatus.SIE`，避免 SIE 已开但 early flag 仍为 true 的窗口。阶段只打开 boot CPU 本地中断总入口；root supervisor external input gate、PLIC UART source enable、周期 tick、完整 softirq 执行、IPI runtime、workqueue workers、RCU GP kthreads、task concurrency 和 SMP concurrency 均保持 closed/deferred。规格侧补齐无 `within` 事实、Linux 顺序事实和 ready invariant 的负向断言；coding 侧补 mandatory requirement；实现侧修正 `InterruptType.enable()` 顺序，并在 `local_irq_enable_phase_ready()` 中确认 external/root gate、PLIC source、IPI、softirq、workqueue 和 RCU GP 仍 deferred。验证命令为 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`。

当前进度：第 7 项 `IrqOpenPreparePhase` 已按用户指定直接执行 `/tmp/subphase_audit_workflow.md` 并完成复核和补齐。本阶段 Linux 对照边界固定为 `init/main.c::start_kernel()` 中 `kmem_cache_init_late()` 到 `arch_cpu_finalize_init()`，下一行 `pid_idr_init()` 属于 `ProcessPreparePhase`；关键调用顺序为 `kmem_cache_init_late()`、`console_init()`、`if (panic_later) panic(...)`、`lockdep_init()`、`locking_selftest()`、`CONFIG_BLK_DEV_INITRD` 下 initrd bounds 检查、`setup_per_cpu_pageset()`、`numa_policy_init()`、`acpi_early_init()`、`late_time_init` hook、`sched_clock_init()`、`calibrate_delay()` 和 `arch_cpu_finalize_init()`。当前 `.config` 为 `CONFIG_SLUB=y`、`CONFIG_SLUB_TINY=n`、`CONFIG_PRINTK=y`、`CONFIG_TTY=y`、`CONFIG_SERIAL_8250_CONSOLE=y`、`CONFIG_GENERIC_SCHED_CLOCK=y`、`CONFIG_DEBUG_LOCK_ALLOC=n`、`CONFIG_DEBUG_LOCKING_API_SELFTESTS=n`、`CONFIG_BLK_DEV_INITRD=n`、`CONFIG_NUMA=n`、`CONFIG_ACPI=n`，且 RISC-V 未启用 `CONFIG_ARCH_HAS_CPU_FINALIZE_INIT`。复核结论：阶段外层复用 `SingleTaskInterruptTypeContext`，boot CPU 本地中断总入口已经打开，但任务并发、SMP 并发、workqueue worker、RCU GP kthread、完整 softirq 执行、周期 tick 和 IPI runtime 仍关闭或 deferred；本阶段没有新增 mutex、semaphore、completion、waitqueue、rwlock、percpu rwsem、RCU/SRCU、CPU bring-up 同步量、TLB/cache flush 或 text patch 同步边界。必须保留的同步协议是 `sched_clock_init()` 在当前 `../linux-6.12/.config` 的 RISC-V 配置下围绕 `generic_sched_clock_init()` 的 `local_irq_disable()` / `local_irq_enable()`，已用 `within SchedClockLocalInterruptContext { ... }` 表达并映射到 `BootCpuLocalInterrupt`；`kmem_cache_init_late()` 只建立 SLUB flush workqueue 事实，不启动 worker 或推进 `SlubSubsystem.Online/FULL`；`setup_per_cpu_pageset()` 是 `PageAllocator` per-CPU pageset 快速路径 deferred 事实，不误写成 no-op。规格侧新增 `IrqOpenPrepareTrimmedPaths` 两段式对象，结构化记录 `panic_later` 清理、lockdep/selftest、initrd/NUMA/ACPI/late_time/arch finalize 裁剪依据和 `setup_per_cpu_pageset()` deferred；coding 侧新增 mandatory requirement；实现侧新增对应对象、checkpoint、ready check 和 smoke 观测，并把 `SchedClock.setup()` 从 irqsave/restore 事实修正为 disable/enable guard。验证命令为 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`；本轮普通 `make run` 输出 `Hello, world!`，普通 `APP=user-boot` 输出 `user hello` / `user exit status=0`。

当前进度：第 8 项 `ProcessPreparePhase` 已按用户指定直接执行 `/tmp/subphase_audit_workflow.md` 并完成复核和补齐。本阶段 Linux 对照边界固定为 `init/main.c::start_kernel()` 中 `pid_idr_init()` 到 `kcsan_init()` 完成后、`rest_init()` 执行前；关键调用顺序为 `pid_idr_init()`、`anon_vma_init()`、x86-only `efi_enter_virtual_mode()`、`thread_stack_cache_init()`、`cred_init()`、`fork_init()`、`proc_caches_init()`、`uts_ns_init()`、`key_init()`、`security_init()`、`dbg_late_init()`、`net_ns_init()`、`vfs_caches_init()`、`pagecache_init()`、`signals_init()`、`seq_file_init()`、`proc_root_init()`、`nsfs_init()`、`pidfs_init()`、`cpuset_init()`、`cgroup_init()`、`taskstats_init_early()`、`delayacct_init()`、`acpi_subsystem_init()`、`arch_post_acpi_subsys_init()` 和 `kcsan_init()`。当前 `.config` 为 `CONFIG_PID_NS=y`、`CONFIG_UTS_NS=y`、`CONFIG_NET_NS=y`、`CONFIG_PROC_FS=y`、`CONFIG_KEYS=y`、`CONFIG_SECURITY=y`、`CONFIG_UPROBES=y`、`CONFIG_VMAP_STACK=y`、`CONFIG_PER_VMA_LOCK=y`、`CONFIG_SMP=y`、`CONFIG_PREEMPT_COUNT=y`，且 `CONFIG_TASKSTATS=n`、`CONFIG_CGROUPS=n`、`CONFIG_ACPI=n`、`CONFIG_KCSAN=n`、`CONFIG_KGDB=n`、`CONFIG_SHADOW_CALL_STACK=n`、`CONFIG_PROVE_LOCKING=n`。复核结论：阶段外层仍是 boot CPU 中断已打开但任务并发、SMP 并发、workqueue worker、RCU GP kthread 和完整 softirq 执行关闭或 deferred；本阶段没有新增 local IRQ guard、preemption guard、raw spinlock、mutex、semaphore、completion、rwlock、percpu rwsem、RCU/SRCU 读侧或 GP 侧、TLB/cache flush、text patch 同步或 AP/BP 同步量。需要显式保留的同步边界是：`fork_init()` 中 `CPUHP_BP_PREPARE_DYN` 仅注册后续 `free_vm_stack_cache` hotplug callback，不执行 AP/BP 同步；`anon_vma_init()` 建立带 `rwsem` ctor 和 `SLAB_TYPESAFE_BY_RCU` 的 cache，但匿名 VMA graph/runtime RCU 语义 deferred；`key_init()` 只初始化 key cache、内建 key type list 和 root key user tracking，运行期 `key_types_sem` 写侧协议 deferred；`security_init()` 的 LSM 顺序和 hook dispatcher 只发布启动期 registry facts，可选 LSM 细节 deferred；`pagecache_init()` 的 folio waitqueue table、writeback、`proc_root_init()`/`nsfs_init()`/`pidfs_init()` 的 pseudo filesystem mount 以及 `vfs_caches_init()` 内 `bdev_cache_init()`/`chrdev_init()` 均保持显式 deferred。`rcu_init_tasks_generic()` 不在本阶段，它位于 `rest_init()` 之后的 `kernel_init_freeable()`，本轮记录为 out-of-scope。规格侧新增 `ProcessPrepareTrimmedPaths`，结构化记录 x86 EFI/SCS/lockdep/KGDB/cpuset/cgroup/taskstats/delayacct/ACPI/KCSAN 裁剪依据，以及 net namespace、page cache、seq/proc/nsfs/pidfs、bdev/chrdev 和 RCU Tasks 的 deferred/out-of-scope 事实；coding 侧新增 mandatory requirement；实现侧新增 `ProcessPrepareTrimmedPaths` 对象、`Config` accessor、checkpoint、ready check 和 process_prepare smoke 观测。未新增 formal context 或 `within`，原因是本阶段没有新的词法 guard 临界区，复用阶段上下文和结构化 deferred facts 即可准确表达。验证命令为 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`；本轮普通 `make run` 输出 `Hello, world!`，普通 `APP=user-boot` 输出 `user hello` / `user exit status=0`。

当前进度：`FinalizePhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核和补齐。本阶段 Linux 对照边界固定为 Linux 6.12 `init/main.c::kernel_init()` 中 `kernel_init_freeable()` 返回后，从 `async_synchronize_full()` 到 `do_sysctl_args()` 返回，下一行 `run_init_process()` 属于 `PayloadPhase`；关键调用顺序为 `async_synchronize_full()`、`system_state = SYSTEM_FREEING_INITMEM`、`kprobe_free_init_mem()`、`ftrace_free_init_mem()`、`kgdb_free_init_mem()`、`exit_boot_config()`、`free_initmem()`、`mark_readonly()`、`pti_finalize()`、`system_state = SYSTEM_RUNNING`、`numa_default_policy()`、`rcu_end_inkernel_boot()` 和 `do_sysctl_args()`。当前 `.config` 为 `CONFIG_TREE_RCU=y`、`CONFIG_PREEMPT_RCU=y`、`CONFIG_SYSCTL=y`、`CONFIG_PROC_SYSCTL=y`、`CONFIG_NUMA=n`、`CONFIG_KPROBES=n`、`CONFIG_KGDB=n`、`CONFIG_BOOT_CONFIG=n`、`CONFIG_ARCH_HAS_STRICT_KERNEL_RWX=y`、`CONFIG_STRICT_KERNEL_RWX=y`、`CONFIG_DEBUG_RODATA_TEST=n`、`CONFIG_PREEMPT_RT=n`、`CONFIG_RCU_EXPERT=n`。复核结论：本阶段处在 `KernelInitTask` 执行线，任务调度和 boot CPU 本地中断已可用，但没有新增词法 mutex/semaphore/completion/rwlock/percpu rwsem/RCU read-side/TLB flush/text patch guard；因此未新增 formal context，也不新增 `within`。需要显式保留的同步责任是 `async_synchronize_full()` 经 `wait_event(async_done, lowest_in_progress(NULL) >= ASYNC_COOKIE_MAX)` 等待全局 async work drain，`lowest_in_progress()` 使用 `async_lock` 的 `spin_lock_irqsave()`，async worker 完成路径使用 `entry_count` atomic 并 `wake_up(&async_done)`；当前 `AsyncCore` runtime 不展开，这些 waitqueue、spinlock irqsave、atomic 和 cookie ordering 责任均记录为 deferred facts。`rcu_end_inkernel_boot()` 中 `rcu_unexpedite_gp()` 的 atomic decrement、`CONFIG_RCU_LAZY` 未启用下 `rcu_async_relax()` 裁剪、`CONFIG_PREEMPT_RT=n` 下 `rcu_normal_after_boot` 默认不触发 `WRITE_ONCE(rcu_normal, 1)`，以及 `rcu_boot_ended` publish fact 已纳入实现观测。规格侧新增 `NumaDefaultPolicyTrimmed`，把 `numa_default_policy()` 的 `CONFIG_NUMA=n` 裁剪事实从误放的 rest_init 路径语义移到 `SYSTEM_RUNNING` 之后、`rcu_end_inkernel_boot()` 之前；coding 侧补强 async/RCU/NUMA mandatory requirement；实现侧新增 `NumaDefaultPolicyTrimmed` 对象、扩展 `AsyncFullSyncDeferred` 与 `RcuCore` facts，并由 finalize smoke 观测。验证命令为 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`。

### 已完成当前轮：rest_init 三个 owner-scoped 子阶段规格/实现补齐

`rest_init()` 路径已拆为 `BootInitRestInitPhase`、`BootInitScheduleHandoffPhase` 和 `BootIdleEntryPhase` 三个子阶段，规格侧已补齐 `within` 形式的上下文边界。本轮已把三个子阶段的 `impl/arceos_ex` 实现与已确认规格对齐，并补 smoke 观测。`RestInitPhase` wrapper 对象、状态和 checkpoint 不再作为正式身份保留。

1. **第 1 子阶段：`BootInitRestInitPhase` 实现补齐完成**。`RcuCore.scheduler_start()` 已显式消费 `BootCpuLocalInterrupt` 的 save/restore guard，记录 scheduler-starting local IRQ 事实；`KernelInitTask.PinToBootCpu` 和 `KthreaddTask.BindGlobalRef` 已在 `BootIdleRcuReadSide` 读侧上下文内完成 PID lookup / global ref 事实；`KthreaddReadyGate.Complete` 已以独立 `KthreaddReadyGateWaitLock` 承载 `complete()` 的 wait.lock 临界区，只发布 completion token/wake 事实；PID 1 的 release fact 由后续 `KernelInitTask` wait side 观察后提交，并由 smoke 验证这些同步事实。
2. **第 2 子阶段：`BootInitScheduleHandoffPhase` 实现补齐完成**。`Scheduler.schedule()` / `switch_to()` 已补齐当前规格中的 schedule handoff 事实：schedule 自身的抢占上下文、runqueue lock guard、`smp_mb__after_spinlock()` 对应事实、rq clock update、`need_resched` clear、`rq->curr` RCU publish、`trace_sched_switch`、`prepare_task_switch()` / `finish_task_switch()`、finish 阶段释放 rq lock 和恢复 preempt count 的观测；scheduler/rest_init smoke 已覆盖这些事实。
3. **第 3 子阶段：`BootIdleStartupPhase` 规格/实现补齐完成**。对照 Linux `cpu_startup_entry()` / `do_idle()` 后，已新增 `BootIdleWaitLocalInterruptContext`，把 idle wait 代表性迭代中的 local IRQ disabled 区用 `within` 表达；规格和实现已记录 `nohz_run_idle_balance()`、polling rmb、`arch_cpu_idle_enter/exit()`、RCU nocb deferred wakeup flush、offline-dead path 未进入、poll/cpuidle/WFI deferred、`preempt_set_need_resched()`、清 polling 后 `smp_mb__after_atomic()`、`flush_smp_call_function_queue()` 和 livepatch state update deferred。rest_init smoke 已覆盖这些新增事实。
4. **验收完成**。前两个子阶段补齐时已覆盖 `make verify`、`make verify REPORT=graph`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run` 和普通 `make run APP=user-boot`。`APP=user-boot` 首次复跑因旧 `build/virtio-blk.raw` 未应用 rootfs overlay 输出 `read user ELF failed`；执行 `make -C impl/arceos_ex disk FORCE=1` 重建磁盘后，普通 `make run APP=user-boot` 输出 `user hello` / `user exit status=0`。第 3 子阶段补齐后重新运行 `make verify`、`make verify REPORT=graph`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run` 和普通 `make run APP=user-boot` 均通过；`APP=user-boot` 输出 `user hello` / `user exit status=0`。

### 已完成当前轮：PreSmpInitPhase 审核与 kthreadd_done wait-side 收敛

`PreSmpInitPhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核。Linux 对照边界固定为 `init/main.c::kernel_init()` 中 `wait_for_completion(&kthreadd_done)` 开头，随后进入 `kernel_init_freeable()`，覆盖 `gfp_allowed_mask = __GFP_BITS_MASK` 到 `smp_init()` 调用前的初始化段；`smp_init()` 本身属于后续 `SmpBringupPhase`。当前 `.config` 为 `CONFIG_SMP=y`、`CONFIG_HOTPLUG_CPU=y`、`CONFIG_NUMA=n`、`CONFIG_CGROUPS=n`、`CONFIG_PREEMPT=y`、`CONFIG_PREEMPT_RCU=y`、`CONFIG_TASKS_RCU=y`、`CONFIG_LOCKUP_DETECTOR=y`、`CONFIG_SOFTLOCKUP_DETECTOR=y`、`CONFIG_HARDLOCKUP_DETECTOR=n`、`CONFIG_PROC_FS=y`、`CONFIG_VM_EVENT_COUNTERS=y`、`CONFIG_FTRACE=y`。复核结论：本子阶段是首次从 `KernelInitTask` 视角进入的新任务执行线；入口不是 BootTask 继续调用，而是 PID 1 在首次调度后通过 wait side 观察 `kthreadd_done` / `KthreaddReadyGate` 释放事实。`KthreaddReadyGate.complete()` 只发布 completion token/wake 事实，`KernelInitKthreaddDoneWait` 在 `PreSmpInitPhase` 第一驱动中消费或观察该 token 并提交 `KernelInitTask` release fact，因此 complete 早于 wait 时可 fast observe，wait 早于 complete 时可阻塞后最终观察释放。上下文方面，任务调度、completion/kthread、阻塞分配和本地中断已处于可用语境，`PageAllocatorFullGfpMask` 在入口 wait 后打开；secondary CPU 仍只到 possible/present，SMP 多核并发保持关闭直到 `SmpBringupPhase`。本轮补齐 `Workqueue.Transition::Setup` 对 Linux `workqueue_init()` 的 `wq_pool_mutex` guard：规格复用 `within WorkqueuePoolMutexContext { ... }`，实现侧由 `KernelInitTask` owner 执行 lock/unlock 并记录 `workqueue_init_pool_mutex_guard_used`；smoke 覆盖 KernelInit wait-side release 和 Workqueue pool mutex 计数。`set_mems_allowed()` 在当前 NUMA/cgroup 配置下为裁剪路径；`cad_pid`、procfs/vmstat 导出、lockup detector 细节、Workqueue/RCU 运行期和 `smp_init()` 后续行为保持 deferred 或下一子阶段边界。验证覆盖 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run`、普通 `make run APP=user-boot` 和 `git diff --check`；普通 `make run` 输出 `Hello, world!`，普通 `APP=user-boot` 输出 `user hello` / `user exit status=0`。

### 已完成当前轮：SmpBringupPhase 审核与 CPU hotplug 同步补齐

`SmpBringupPhase` 已按 `/tmp/subphase_audit_workflow.md` 口径完成复核。Linux 对照边界固定为 Linux 6.12 `kernel/smp.c::smp_init()`，从 `idle_threads_init()` 开始，经 `cpuhp_threads_init()`、`bringup_nonboot_cpus(setup_max_cpus)`、online count checkpoint，到 RISC-V `arch/riscv/kernel/smpboot.c::smp_cpus_done()` 返回结束；RISC-V `smp_cpus_done()` 当前为空实现，按 trimmed/no-op 记录。当前 `.config` 为 `CONFIG_SMP=y`、`CONFIG_HOTPLUG_CPU=y`、`CONFIG_HOTPLUG_PARALLEL=n`、`CONFIG_HOTPLUG_SPLIT_STARTUP=n`、`CONFIG_NUMA=n`、`CONFIG_PREEMPT=y`、`CONFIG_PREEMPT_COUNT=y`、`CONFIG_PREEMPT_RCU=y`、`CONFIG_TASKS_RCU=y`、`CONFIG_RISCV_SBI=y`、`CONFIG_RISCV_BOOT_SPINWAIT=n`、`CONFIG_RISCV_ISA_V=y`、`CONFIG_GENERIC_IRQ_IPI_MUX=y` 和 `CONFIG_GENERIC_CLOCKEVENTS_BROADCAST=y`。后续阶段范式审计已把 AP 入口拆成 `ApEntryPreludePhase`、`ApSmpCallinPhase`、`ApOnlineIdlePhase` 三个 replicated family；SmpBringup 仍是 BP-focused 协调边界，不获得 AP phase owner。

复核结论：本阶段涉及 CPU hotplug、SMP bring-up、completion、mutex、percpu rwsem、raw spinlock irqsave、本地中断和内存顺序。`cpuhp_threads_init()` 中 `smpboot_register_percpu_thread()` 的 `cpus_read_lock()` + `smpboot_threads_lock` mutex 已正式建模并用 `within SmpBringupCpuHotplugReadContext { within SmpbootThreadsMutexContext { ... } }` 表达；`bringup_nonboot_cpus()` / `cpu_up()` 的 `cpu_add_remove_lock` + `cpus_write_lock()` writer 边界已用 `within CpuAddRemoveMutexContext { within CpuHotplugWriteContext { ... } }` 表达；RISC-V `__cpu_up()` 等待 `cpu_running` 和 generic CPUHP 等待 `done_up` 的 completion wait.lock raw spinlock irqsave 边界分别用 `within CpuRunningCompletionWaitLockContext { ... }` 和 `within DoneUpCompletionWaitLockContext { ... }` 表达。实现侧新增 `SmpbootThreadsLock`、`CpuAddRemoveLock`、`CpuRunningWaitLock`、`DoneUpWaitLock` 观测对象/计数，并把 `CpuHotplugSyncSet`、`CpuStartProvider`、`SecondaryCpuStartupAck`、`SecondaryCpuOnlineAck` ready checks 扩展到这些 guard facts。

当前 formal 路径覆盖 BP 侧 `idle_threads_init()`、`cpuhp_threads_init()`、串行 CPUHP bringup、`cpu_ops_sbi.cpu_start()` boot data publish ordering、`cpu_running` / `done_up` completion 观察、secondary CPU online 和 `smp_concurrency_open`。`CONFIG_HOTPLUG_PARALLEL=n` 使 parallel bringup 为 trimmed/no-op；`setup_max_cpus == 0` 仍只作为 nosmp checkpoint，不在当前 default path 触发。AP 三阶段现已覆盖 per-logical-id 四态、pointwise sibling continuation、boot-data/SP/TP adoption 和独立 WFI park entry；AP live `CurrentCPU` / `InterruptType`、真实 `riscv_ipi_enable()` / local IRQ guard、完整 cache/TLB flush、hotplug thread callback 和 rollback/teardown 继续保持 summary/deferred。BP 在 HSM 前重置 family 并 release 发布 prerequisite，HSM 后以 Acquire 等待全部 Online，随后才观察 completion、发布 CPU online 和 SMP concurrency。smoke 覆盖 BP guard/completion 主线以及每个 secondary 的三阶段 Online、boot-data/SP/TP 和 park-loop 事实；SMP2/SMP8 announce 覆盖 AP owner、同 AP 严格顺序、不同 AP 交错和 all-online barrier。

### 已完成专题：RISC-V VMAP trap 栈 early overflow

boot CPU 上 `KernelInitTask/UserAppFlow` 的首个 VMAP kernel trap stack 现已完成 Linux 6.12 对齐的 early
overflow 闭环。`APP=user-boot` 的 `formal_event_entry` 在用户来源时继续从 `sscratch` 取得安全的
kernel stack top 并绕过 bit-test；内核来源时在保存任何通用寄存器前只使用 `sp`/`sscratch`，按
`((sp - 288) >> 14) & 1` 分类 prospective frame。正常分支恢复原 `sp`、清零 `sscratch` 后进入
共享 frame 保存；溢出分支通过 `t6`/`sscratch` 保留坏栈 SP 和原始 `t6`，切换到链接可见、16
字节对齐的 4 KiB 静态 overflow stack，保存完整整数寄存器与 `sepc/scause/stval/sstatus` 后，只经
SBI 输出 task/overflow stack 范围和 CSR 诊断并终止系统。普通 app 保持原入口，per-task owner、
per-CPU overflow stack 与 IRQ hardirq `call_on_irq_stack()` 仍是活跃后续责任。

验证覆盖 `make verify`、共享 object smoke 55/55、真实 user-smoke callee-saved sentinel `ecall`、
随机用户栈顶下 `KernelInitTask/UserAppFlow.EnterUserMode` checkpoint facts、DF-0002 ordinary 30/30、完整默认
stress 的 DF-0001/0002/0003 各 10/10、默认 rc.local 与 Linux exact baseline difftest 各 1/1，
以及仓库根直接 `make test`。不可恢复分支没有测试专用 syscall/boot 参数/checkpoint；端点分类、
frame 构造和普通 trap 往返由共享契约验证。

### 最高优先级：状态机术语与 guard lowering 策略收敛

全局语义收敛已经完成，`SchedInitPhase` 当前轮已按 rest_init owner-split 路径的“Linux 对照 -> 先规格 -> 后实现补齐 -> 完整验证”口径收尾。下面第 6-10 项保留首轮材料来源，第 11 项记录当前轮完成结果；后续当前重点切换为启动阶段锁/同步复核记录回填。

1. **P0 已完成：完成 `.spec` 与工具链向 `Transition` 的硬迁移**。对象 process 语法中的旧事件命名已删除，不再保留兼容入口：对象状态转移块统一写作 `transitions`，状态推进统一写作 `Transition`，对象驱动引用统一写作 `Object.Transition::Name`；`Action::X` 保持不变，真正外部事件、硬件事件和 trace event 的自然语言用法可保留但不得再表示对象 process。已更新 parser/AST/model JSON/derive/view/render 内部 schema、默认 target、`spec/**/*.spec`、工具测试 fixture、文档和 trace/render 期望，并删除旧 parser 兼容。
2. **P0 已完成：规范化状态机概念和术语**。`spec/charter/main.md` 与 `spec/model/SEMANTICS.md` 已把核心术语收敛为：`State` 表示生命周期状态，`Transition` 表示改变生命周期状态或扩展状态的转移过程，`Action` 表示不改变当前被建模状态的操作，`Event` 保留给真实外部事件、异步事件、硬件事件或 trace event。当前 P0 将完成 `.spec` 源语法和工具 schema 的硬迁移。
3. **P0 已完成：暂缓基于 Effective Context 的 guard 优化省略**。`BootPhaseContext` / Effective Context 只提供当前调用点的上下文事实，不再作为默认省略 protocol guard 的依据。已保留 parser/model 对 `within ... only-once` 的支持能力，但从正式规格使用点移除了 `only-once` 标记；当前 guard lowering 按非优化路径执行，除非某个 guard 本身就是无运行时代码的纯上下文事实。
4. **P0 已完成：同步 model/coding 规则和现有说明**。当前映射见 `spec/coding/objects/effective-context.md` 和对应 phase coding `.md`：现阶段不依赖 `only-once` 做 guard 省略；CorePrepare 中 `ResourceLock`、`CpuHotplugLock`、`JumpLabelMutex` 和 `PrintkBuffer` 的实现映射已切换为保守执行/保留协议边界，正式模型中的 proof-only predicate 残留已移除。
5. **P0 已完成：验证并提交后再恢复启动阶段审计**。Transition 硬迁移已运行工具单测、`make verify`、`make test`、`make run`、普通 `make run APP=user-boot` 和 `git diff --check`，提交为 `c86a5d2`。当前继续以本 roadmap、`spec/charter/main.md`、`spec/model/boot/sched-init/` 和 `spec/coding/arceos_ex.*` 为 `SchedInitPhase` 审计依据；旧的 `/tmp/sched_init_audit_steps.md` 临时记录不再作为持久计划来源。
6. **P0 已完成首轮：确认 `SchedInitPhase` Linux 边界与 `.config` 分类**。已固定 `start_kernel()` 中 `mm_core_init()` 返回后到 `context_tracking_init()` 后的调用边界，并明确 `early_irq_init()` 是下一阶段入口；`poking_init()`、`ftrace_init()`、`early_trace_init()`、`sched_init()`、IRQ-disabled check、`radix_tree_init()`、`maple_tree_init()`、`housekeeping_init()`、`workqueue_init_early()`、`rcu_init()`、`trace_init()`、`initcall_debug_enable()` 和 `context_tracking_init()` 已按当前 `../linux-6.12/.config` 分类为 formal、checkpoint、trimmed/no-op 或 deferred。当前复核结论：`.config` 启用 `CONFIG_FTRACE=y` / `CONFIG_TRACING=y`，但未启用 `CONFIG_FTRACE_MCOUNT_RECORD`，因此 `ftrace_init()` 在 `include/linux/ftrace.h` 折叠为空调用；启用 `CONFIG_CONTEXT_TRACKING=y` / `CONFIG_CONTEXT_TRACKING_IDLE=y`，但未启用 `CONFIG_CONTEXT_TRACKING_USER_FORCE`，因此本调用点的 `context_tracking_init()` 折叠为空实现，idle/user/EQS 运行期语义留给后续 RCU/context tracking 消费路径。
7. **P0 已完成首轮：收敛 `CpuGroup` 与 `DefaultSchedRootDomain` 规格/实现**。已在 charter、model 和 coding 中明确：每个 logical CPU 对应统一 `Cpu` 类型实例，`CpuGroup` 维护 logical-id 到 `CpuRef` 的索引以及 possible/present/online set 视图；`DefaultSchedRootDomain` 是 Scheduler 的调度覆盖视图，覆盖 `CpuGroup.possible_cpus` 的 `CpuRef` 集合，不拥有 CPU 本体。实现侧已移除独立 `CpuIdMap`，并新增 `DefaultSchedRootDomain` 代码文件和 smoke 覆盖。
8. **P0 已完成首轮：建立 possible CPU runqueue 元数据视图**。提交 `fe05d3a` 已让 `Scheduler` 按 logical-id 暴露 possible CPU runqueue metadata：logical-id 0 与 `BootRunQueue` 对齐，其它 possible CPU 只建立 runqueue 元数据和 root-domain attach 事实，不创建 AP CurrentCPU 或 runnable AP flow。smoke 已覆盖 `CpuGroup`、`DefaultSchedRootDomain` 和 runqueue 元数据的一致性。
9. **P0 已完成首轮：落实 CPU-owned RunQueue/IdleTask lowering**。Boot CPU 的 `CpuGroup.Cpu[0].RunQueue` / `CpuGroup.Cpu[0].IdleTask` 正式 view 已落地，`sched_init` ready、scheduler checkpoint 和 smoke 已通过 `CpuOwnedSchedulerView` 观察 boot CPU-owned 关系。rest_init 中 `KernelInitTask` / `KthreaddTask` 的 CPU 归属来源已收敛到 selected runqueue ref 的 `cpu_id()`，不再从 `Scheduler.boot_runqueue()` 反推 task CPU；`CurrentRunQueueRef` 解析已按规格收敛为 `CurrentTaskRef -> Task.cpu_id -> CpuGroup.Cpu[id].RunQueue`，`CpuGroup.boot_cpu()` 只作为 boot 选择/一致性校验使用。`Scheduler.Action::SelectRunQueue` lowering 返回独立 `RunQueueRef`，调用侧再从 selected_rq 设置 task CPU 并入队；`CurrentRunQueueRef` 只保留给 schedule/pick-next 的当前 CPU 私有路径。rest_init phase/checkpoint 对 boot runqueue membership/count 的观察已改为通过 `CpuOwnedSchedulerView` 投影事实完成，`TaskCreationCore.copy_process()` 的 scheduler/runqueue 前置条件也已通过传入 `CpuGroup` 后验证 CPU-owned scheduler view。scheduler/rest_init/sync primitive smoke 对 CPU-owned runqueue membership 和 idle task thread-context facts 的观察已转向 `CpuOwnedSchedulerView`/`CpuIdleTaskView`；仅保留明确的 transitional storage parity 对照。`BootRunQueueRef` 已收敛为 boot-backed UP storage/lowering detail，公开构造与检查使用 CPU-owned runqueue 语义；secondary idle task 仍留给 SMP bringup，不在本步提前迁移。
10. **P0 已完成首轮：结构化记录 `SchedInitPhase` trace / housekeeping / context tracking 裁剪边界**。coding/model 已修正为按配置区分：`ftrace_init()` 和 `context_tracking_init()` 是当前 `../linux-6.12/.config` 的 RISC-V64 配置下的 trimmed/no-op call point，并由 `SchedInitPreludeTrimmedPaths` / `SchedInitTraceContextBoundaries` 边界对象记录 `CONFIG_FTRACE_MCOUNT_RECORD=n`、`CONFIG_CONTEXT_TRACKING_USER_FORCE=n` 和调用位置保留事实；`early_trace_init()`、`trace_init()` 和 `housekeeping_init()` 仍是显式 deferred 边界。实现侧已补对应对象和 scheduler smoke 观察。
11. **P0 已完成当前轮：按 rest_init owner-split 口径补做 `SchedInitPhase` 完整审核**。本轮没有新增 formal 子阶段；只在现有 `SchedInitPhase` 内对齐 Linux `start_kernel()` 中 `mm_core_init()` 返回后到 `early_irq_init()` 前的真实调用边界。`SchedInitPreludeTrimmedPaths` 记录 `poking_init()`、`ftrace_init()` 和 `early_trace_init()` 的 scheduler 前边界，`SchedInitTraceContextBoundaries` 记录 `trace_init()` 和 `context_tracking_init()` 的 RCU 后边界；当前配置下 `ftrace_init()` / `context_tracking_init()` 为 trimmed/no-op，`early_trace_init()` / `trace_init()` / `housekeeping_init()` 保持显式 deferred。实现侧已补对应对象、checkpoint 和 scheduler smoke 观测，完整验证覆盖 `make verify REPORT=graph`、`make -C impl/arceos_ex run APP=smoke`、普通 `make run` 和普通 `make run APP=user-boot`。
12. **P0 已完成：8 个启动边界锁/同步复核记录回填与缺口确认**。已按阶段树从前往后复核迁移前 `EntryPreludePhase`（现 `BootInitFlow.Preset` 入口步骤）、`EntrySuccessorPhase`、`CorePreparePhase`、`MmCoreInitPhase`、`IrqTimeInitPhase`、`LocalIrqEnablePhase`、`IrqOpenPreparePhase`、`ProcessPreparePhase`。第 1 项入口步骤已完成当前轮复核并补齐 `apply_early_boot_alternatives()` deferred/ready 事实；第 2 项 `EntrySuccessorPhase` 已完成当前轮复核并补齐 `init_vmlinux_build_id()`、`page_address_init()`、RISC-V `setup_bootmem()` 和 `SwapperVm` RWX 权限拆分 deferred/ready 事实；第 3 项 `CorePreparePhase` 已完成当前轮复核，确认现有 `within` guard、deferred/trimmed/checkpoint 分类和实现观测已经覆盖当前固定检查面；第 4 项 `MmCoreInitPhase` 已完成当前轮复核，确认 zonelist irqsave seqlock、printk deferred、SWIOTLB static lock、SLUB/vmalloc/ioremap runtime deferred contract、trimmed/no-op 路径和实现观测已经覆盖当前固定检查面；第 5 项 `IrqTimeInitPhase` 已完成当前轮复核并补齐 RISC-V IRQ stack/SCS 裁剪、timekeeper raw spinlock irqsave + seqcount、`rcu_init_nohz()` / `kfence_init()` 裁剪、`sbi_ipi_init()` 顺序和 softirq/timekeeping 顺序观测；第 6 项 `LocalIrqEnablePhase` 已完成当前轮复核并补齐清 early flag 后打开 `sstatus.SIE` 的顺序事实和 runtime gates deferred 观测；第 7 项 `IrqOpenPreparePhase` 已完成当前轮复核并补齐结构化 trimmed/deferred facts 与 sched clock local IRQ disable/enable guard；第 8 项 `ProcessPreparePhase` 已完成当前轮复核并补齐 `ProcessPrepareTrimmedPaths`，结构化记录当前配置裁剪、启用但 deferred 的 pagecache/proc/nsfs/pidfs/net namespace 路径和 `rcu_init_tasks_generic()` out-of-scope 事实。`SchedInitPhase` 和 rest_init owner-split 路径保持已完成当前轮状态。
13. **P0 后续：拆分 Linux tracing 与 housekeeping 后续边界**。继续本项时，应先确定 Linux tracing core 与项目 checkpoint announce / Linux-like trace 的关系，再 formal `housekeeping_init()` 的 flags/cmdline mask/domain 边界，避免把启用配置下的真实责任误归为永久 no-op。

### 已完成专题：Effective Context 与 CorePrepare 同步原语缺口

以下各项作为当前启动阶段审计的最高优先级。`Effective Context` 已先在 `spec/charter/main.md` 形成首轮概念结论，下一步必须落到 `spec/model`、`spec/coding` 正式规格中，作为 Mutex、RwLock、RawSpinLock、InterruptType、RCU 等 guard 建模和代码生成约束的共同前提。修复顺序仍遵循先规格、再 coding 映射、最后确认 `impl/` 生成/实现结果的原则。

1. **P0 已完成首轮：将 Effective Context 结论正式落到 model 语义和 DSL**。已修订 `spec/model/SEMANTICS.md`：源规格以 `guard` / `guard.holds` 表达上下文为何成立和该 guard 明确保持的属性；`effects` 不再作为 `Context` / `ResourceExclusiveContext` 必填源块，而是由 `guard.kind`、`guard.holds`、`obj_refs` 和上下文类型推导出的归一化结果。缺省维度表示 neutral/inherited，不得用 `true` 表示“未改变/未约束”；上下文嵌套仍按单调加强规则检查。下一步进入工具和具体 `.spec` 迁移。
2. **P0 已完成首轮：增强 model 工具对 guard-derived Effective Context 的支持**。parser/AST/JSON 已正式承载 `guard.holds`，model builder 已支持 `PhaseBoundaryGuard`，由 guard schema 与 `holds` 推导 context contribution，`within` 栈按 Effective Context 做单调加强检查；旧 `effects` 在迁移期继续兼容，但会检查其不得弱于或偏离 guard 推导结果。已补 parser/model 测试覆盖无 `effects` 的 `PhaseBoundaryGuard`、`holds` 缺省 neutral/inherited 和嵌套冲突。
3. **P0 已完成首轮：在 BootPhase 中建立 BootPhaseContext 并包围阶段 drives**。已在 `spec/model/boot/phase.spec` 定义 `BootPhaseContext: Context`，使用 `PhaseBoundaryGuard.holds` 明确 `single_cpu`、`single_task`、本地中断关闭、抢占关闭等由启动事实和阶段边界保证的属性，并用 `within BootPhaseContext { ... }` 包围 `BootPhase.Transition::Setup` 中对子阶段的驱动序列。`PhaseBoundaryGuard` 自身不生成额外运行时代码，但会贡献 Effective Context 并参与内部 guard lowering 决策；普通 `Context` 未声明 `obj_refs` 时不作为对象驱动白名单。
4. **P0 已完成首轮：迁移现有 context 示例，消除手写 effects 与 guard 语义漂移**。已修订 rest_init 路径等已有上下文，删除 source-level `effects`，改由 `RawSpinLockIrqSaveGuard`、`PreemptionGuard`、`LocalInterruptGuard` 的 schema 推导 Effective Context contribution。`ScheduleLocalInterruptContext` 现在只由 `LocalInterruptGuard` 明确推出本地中断关闭，不再把外层继承的抢占关闭或主动切换关闭写成自身 effect；旧 `effects` 只保留为工具迁移期兼容输入。
5. **P0 已完成首轮：把 Effective Context 到代码生成的 lowering 规则落到 coding 映射**。当前权威正文位于 `spec/coding/objects/effective-context.md`：`PhaseBoundaryGuard` 自身不生成运行时 enter/exit 代码，但会贡献 Effective Context 并参与内部 guard lowering 决策；`PreemptionGuard` 必须保留 counted preemption enter/exit 或等价 RAII 语义，不能因外层已关闭抢占而省略本层计数协议；`LocalInterruptGuard` 的 irqsave/irqrestore 必须保存并恢复原始 flags，不得退化为无条件 disable/enable；`RawSpinLockIrqSaveGuard` 必须执行 lock irqsave / unlock irqrestore 协议。Effective Context 可以影响无运行时代码的 context source 和属性脚手架，但不得擦除带 owner 状态、嵌套计数、saved flags、资源协议、内存顺序或调试语义的 guard。Mutex、RwLock、RCU 的最终 lowering 仍等对应原语规格化后再定。
6. **P0 已完成首轮：正式规格化 Mutex 原语及 JumpLabelMutex 映射**。`StaticBranch.setup()` 对应 Linux `jump_label_init()` 中的 `jump_label_lock()` / `jump_label_unlock()`，其底层实例为 `jump_label_mutex`。通用 `Mutex` 类型、具体 `JumpLabelMutex` 静态实例、`StaticBranchJumpLabelContext`、coding lowering 约束和 `arceos_ex` 首轮实现映射均已完成；`StaticBranch.Transition::Setup` 现在在正式上下文内执行，并由 `JumpLabelMutex.Transition::Lock(BootTaskRef)` / `Unlock(BootTaskRef)` 定界。实现侧以独立 `jump_label_mutex` 对象承载 ready、owner 和 lock/unlock 完成事实，`core_prepare_phase_ready()` 不再只依赖 `StaticBranch` 内部 bool。
7. **P0 已完成首轮：正式规格化 PerCpuRwSemaphore 原语及实现映射**。`cpus_read_lock()` / `cpus_read_unlock()` 底层使用 `DEFINE_STATIC_PERCPU_RWSEM(cpu_hotplug_lock)` 和 `percpu_down_read()` / `percpu_up_read()`。已新增通用 `RcuSync` / `PerCpuRwSemaphore` 类型、`CpuHotplugLock` 静态实例、`CpuHotplugReadContext` 和 coding lowering 约束；实现侧新增独立 `cpu_hotplug_lock` 对象，包含 reader fast path、writer block/drain、RcuSync 和可观测 read/write 行为。`StaticBranch.setup()` 不再只依赖内部 bool，而是由 `CpuHotplugLock` 对象提供 read guard enter/exit 事实，并由 `core_prepare_phase_ready()` 检查正式对象/事件事实。
8. **P0 已完成首轮：明确 PerCpuStorage.Prepared 与 CpuHotplugLock 实例依赖边界**。`PerCpuRwSemaphore` 是通用类型，不全局绑定到 `PerCpuStorage.Prepared`；`CpuHotplugLock` 是该类型的静态实例。`PerCpuStorage.Prepared` 现在表示 `.data..percpu` 静态定义和 boot CPU 早期 per-cpu 访问已经可用，足以支撑 `CpuHotplugLock.Preset`；完整 possible CPU first chunk 和 offset table 仍由 `PerCpuStorage.Ready` 表达。`StaticBranch.setup()` 消费的是 `CpuHotplugLock.Ready` 和 `JumpLabelMutex.Ready`，而不是仅确认静态 initializer 的 `Prepared`。
9. **P0 已完成首轮：正式规格化 RwLock 原语及 ResourceLock 实例**。`ResourceTree.setup()` 对应 Linux `kernel/resource.c` 中的 `insert_resource_conflict()` / `insert_resource()` 路径，`resource_lock` 由 `static DEFINE_RWLOCK(resource_lock)` 定义，类型边界是 `rwlock_t`，使用普通 `write_lock()` / `write_unlock()`，不是 `RawRwLock`。已新增通用 `RwLock` 类型、具体 `ResourceLock` 静态实例、`ResourceTreeWriteContext`、coding lowering 约束和 `arceos_ex` 首轮实现映射；实现侧新增独立 `resource_lock` 对象，`ResourceTree.setup()` 消费该对象并记录 write guard enter/exit；通用 `RwLock` 提供真实 read/write/trylock 行为，并由独立 `RwLock` smoke 覆盖双任务竞争场景。
10. **P0 已完成首轮：补齐 PrintkBuffer.setup 对已有 InterruptType 的使用约束**。`PrintkBuffer.setup()` 对应 Linux `setup_log_buf(0)`，在切换 printk ring buffer 时使用 `local_irq_save(flags)` / `local_irq_restore(flags)`。项目已具备 `BootCpuLocalInterrupt: InterruptType` 以及 `SaveAndDisable` / `Restore` 事件，因此这不是新增同步原语缺口，而是事件 wiring 缺口。已让 `PrintkBuffer.Transition::Setup` 依赖 `BootCpuLocalInterrupt.state == State::Ready` 和 local IRQ disabled 事实，并通过 `within PrintkBufferSetupLocalInterruptContext { ... }` 表达 active buffer switch/copy 的局部临界区；实现侧现在执行 `BootCpuLocalInterrupt.SaveAndDisable` / `Restore`，并让 `core_prepare_phase_ready()` 检查绑定 `BootCpuLocalInterrupt` 的正式 local-interrupt guard 事实，而不是仅检查 `PrintkBuffer` 内部 bool。
11. **P0 已完成首轮：补强 Randomness.Preset 条件锁路径的规格说明**。`Randomness.Preset` 对应 Linux `random_init_early(command_line)`。主路径直接调用 `_mix_pool_bytes()` 混入 early seed material，不经过 `input_pool.lock`，因此不得把 spinlock 写成无条件要求；但末尾 `crng_ready()` / `trust_cpu` 条件路径可能进入 `crng_reseed()` 或 `_credit_init_bits()`，并使用 `base_crng.lock` 的 `spin_lock_irqsave()` / `spin_unlock_irqrestore()`。已补清 model/coding 描述：当前最小实现不展开该条件路径、不要求代码变化；若后续实现条件 reseed/credit-init，再通过既有 `RawSpinLock.LockIrqSave/UnlockIrqRestore` 约束。

### 当前执行计划：Mutex 与 JumpLabelMutex

1. **P0 已完成首轮：对照 Linux 建立通用 `Mutex` 类型规格**。已在 `spec/model/common/main.spec` 中新增 `MutexExtState::{Unlocked, Locked}`、`MutexInitKind::{StaticInitializer, RuntimeInit}` 和通用 `Mutex` 类型；`Mutex` 拥有 `SimpleWaitQueue` 子对象，`Preset` 记录初始化来源和 storage/wait-queue 绑定，`Setup` 建立 unlocked/ready 状态，`Lock(current_task: TaskRef)` / `Unlock(current_task: TaskRef)` 表达独占获取、同 owner 释放、contended block/sleep 和 wake-one 边界。owner 首轮通过 predicate 关系表达，不新增持久 `owner: TaskRef` 字段；wait_lock、optimistic spin、handoff flags、lockdep/debug、ww_mutex 和 interruptible/killable/io variants 保持 deferred/internal。
2. **P0 已完成首轮：定义具体 `JumpLabelMutex` 实例**。已将 Linux `jump_label_mutex` 建模为通用 `Mutex` 类型的具名实例，明确静态 `DEFINE_MUTEX` initializer、storage/init-kind 事实、early unlocked/ready 状态和 wait queue 抽象；`CorePreparePhase.Setup` 现在在 `StaticBranch.Transition::Setup` 前驱动 `JumpLabelMutex.Transition::Preset` / `Setup`，并把 `JumpLabelMutex.state == State::Ready` 纳入阶段 ready invariant。不把完整 sleeping waitqueue、owner tracking、lockdep 等细节作为当前轮硬前置。
3. **P0 已完成首轮：修订 `StaticBranch.setup()` 规格**。已把原先的内部 jump-label mutex guard fact 改为 `StaticBranchJumpLabelContext`：该 context 的 `guard.lock_ref` 指向 `JumpLabelMutex` 对象，`entered_by` / `exited_by` 分别使用 `JumpLabelMutex.Transition::Lock(BootTaskRef)` 与 `Unlock(BootTaskRef)`；`StaticBranch.Transition::Setup` 依赖 `JumpLabelMutex.state == State::Ready` 和 canonical `BootTaskRef`，并在该上下文内提交 static-branch registry、排序、映射和 deferred text-patch 事实。`BootTask` 在入口前即为 Online，`BootTaskRef` 由其静态 identity 与入口 binding 校验保持，表示调度器初始化前 Linux `init_task` 可作为 mutex owner/task ref。
4. **P0 已完成首轮：同步 coding 与实现映射约束**。当前映射见 `spec/coding/phases/boot/core-prepare.md`：`JumpLabelMutex` 作为独立 `Context` 对象出现，`StaticBranch.setup()` 通过 `JumpLabelMutex.Lock` / `Unlock` 或等价 guard 进入/退出 `StaticBranchJumpLabelContext`，并要求 ready-check 观察 mutex 对象 transition 事实。实现侧已新增独立 `jump_label_mutex` 对象，CorePrepare 先执行其 static preset/setup，再由 `StaticBranch.setup()` 以 `BootTask` 为启动期 owner 完成 lock/setup/unlock；`core_prepare_phase_ready()` 同时检查 `JumpLabelMutex` ready、lock/unlock 完成事实和 `StaticBranch` summary fact。

### 当前执行计划：PerCpuRwSemaphore 与 CpuHotplugLock

1. **P0 已完成首轮：更新计划并收敛实现目标**。PerCpuRwSemaphore 当前轮建立完整规格与可观测实现行为；lockdep、trace、精确 waitqueue 调度和真实 RCU grace-period 服务保留为 internal/deferred。`RcuSync` 已作为 `PerCpuRwSemaphore` 的 support type 新增，不复用全局 `RcuCore`。
2. **P0 已完成首轮：建立 common model 支撑**。已在 `spec/model/common/main.spec` 中新增 `RcuSync`、`PerCpuRwSemaphore` 和必要枚举/事实：reader fast path、writer blocking、per-cpu read count、reader drain、waiters、barrier/grace-period facts；事件覆盖 `Preset` / `Setup` / `Enable`、`ReadLock` / `ReadUnlock` / `ReadTryLock` / `WriteLock` / `WriteUnlock`。
3. **P0 已完成首轮：定义具体 `CpuHotplugLock` 实例**。`CpuHotplugLock` 是 `PerCpuRwSemaphore` 类型的静态实例；它的早期可用性依赖 `PerCpuStorage.Prepared`，不把该依赖写到通用类型上。完整多 CPU per-cpu read counter 寻址能力再依赖 `PerCpuStorage.Ready`。`StaticBranch.setup()` 消费的是 `CpuHotplugLock.Ready` / `JumpLabelMutex.Ready`，而不是仅能表示静态 initializer 已确认的 `Prepared`。
4. **P0 已完成首轮：修订 `StaticBranch.setup()` 的 CPU hotplug read guard**。已把 Linux 顺序表达为 `within CpuHotplugReadContext { within StaticBranchJumpLabelContext { ... } }`，由 `CpuHotplugLock.ReadLock/ReadUnlock` 定界，不再只用 `static_branch_cpu_hotplug_read_guard_used(StaticBranch)` 内部 bool。
5. **P0 已完成首轮：同步 coding 与 `arceos_ex` 实现映射**。`spec/coding/objects/effective-context.md` 和 `spec/coding/phases/boot/core-prepare.md` 已说明 `PerCpuRwSemaphore` / `CpuHotplugLock` guard lowering 规则；实现侧新增 `CpuHotplugLock` 对象，包含 `RcuSync` 子结构和 read/write 行为。`StaticBranch.setup()` 保留 `CpuHotplugReadContext` 源码边界；当前按保守策略执行 `CpuHotplugLock.ReadLock/ReadUnlock` 并记录可观测 enter/exit，通用 `PerCpuRwSemaphore` 提供真实 read/write 行为。
6. **P0 已完成首轮：验证与提交**。已运行 `make verify`、`make test`、`make run`、`make run APP=user-boot`；补充 smoke 覆盖 `PerCpuRwSemaphore` 静态初始化、read lock/unlock、writer block/drain、reader fast/slow path 和两任务竞争场景。

### 当前执行计划：RwLock 与 ResourceLock

1. **P0 已完成首轮：建立通用 `RwLock` 类型规格**。对照 Linux `rwlock_t` / `DEFINE_RWLOCK` / `read_lock()` / `write_lock()`，已在 `spec/model/common/main.spec` 中新增 `RwLockExtState::{Unlocked, ReadHeld, WriteHeld}`、`RwLockInitKind::{StaticInitializer, RuntimeInit}` 和通用 `RwLock` 类型。首轮只要求静态/运行时初始化、Ready/unlocked 状态、read-side 可共享抽象、write-side 排他、trylock outcome 和 unlock 条件；lockdep/debug owner、PREEMPT_RT `rwbase_rt`、irqsave/bh/nested variants 和精确 reader count 保留为 internal/deferred。
2. **P0 已完成首轮：定义具体 `ResourceLock` 静态实例**。`ResourceLock` 对应 Linux `kernel/resource.c` 中的 `static DEFINE_RWLOCK(resource_lock)`，类型边界是公开语义层的 `RwLock`，不是 `RawRwLock`。该实例由 CorePrepare 在 `ResourceTree.Transition::Setup` 前执行 `Preset` / `Setup`，Ready 后才可作为 `ResourceTree.setup()` 的 write guard。
3. **P0 已完成首轮：修订 `ResourceTree.setup()` 写锁上下文**。已把 Linux `insert_resource_conflict()` / `insert_resource()` 的 `write_lock(&resource_lock)` / `write_unlock(&resource_lock)` 表达为 `within ResourceTreeWriteContext { ... }`，由 `ResourceLock.Transition::WriteLock(BootTaskRef)` / `WriteUnlock(BootTaskRef)` 定界，并新增 `resource_tree_resource_lock_write_guard_used(ResourceTree, ResourceLock)` 事实。
4. **P0 已完成首轮：同步 coding 与 `arceos_ex` 实现映射**。coding 已说明 `ResourceLock` 是独立 `Context` 对象；实现侧新增独立 `resource_lock` 对象，并让 `ResourceTree.setup()` 消费该对象。当前按保守策略执行 `ResourceLock.WriteLock/WriteUnlock` 并记录可观测 enter/exit；通用 `RwLock` 提供真实 read/write/trylock 行为。
5. **P0 已完成首轮：验证与提交**。已运行 `make verify`、`make -C impl/arceos_ex build APP=smoke`、`git diff --check`、`make -C impl/arceos_ex run APP=smoke`；补充独立 `RwLock` smoke，覆盖静态初始化、read/read 共享、write 排他、trylock 和两任务竞争场景。

### 当前执行计划：Randomness.Preset 条件锁路径

1. **P0 已完成首轮：补强 model 事实**。`Randomness.Transition::Preset` 已明确 Linux `random_init_early()` 的主线使用内部 `_mix_pool_bytes()`，不经过 `mix_pool_bytes()` 的 `input_pool.lock`；不得把 `input_pool.lock` 写成无条件 guard。
2. **P0 已完成首轮：记录条件路径 deferred**。`crng_ready()` 进入 `crng_reseed()`、或 `trust_cpu` 进入 `_credit_init_bits()` 时，后续可能触发 `base_crng.lock` 的 `spin_lock_irqsave()` / `spin_unlock_irqrestore()`。当前最小规格只记录该条件路径未展开；若将来展开，必须通过既有 `RawSpinLock.LockIrqSave/UnlockIrqRestore` guard 建模。
3. **P0 已完成首轮：同步 coding 约束**。`arceos_ex` 当前实现不得为了满足 `Randomness.Preset` 无条件生成 input-pool/base-crng 锁代码；只有实现条件 reseed/credit-init 路径时才需要生成对应 irqsave 自旋锁协议。
4. **P0 已完成首轮：验证**。本轮为规格描述补强，已运行 `make verify` 和 `git diff --check`；不触发实现变更。

### 当前执行计划：CorePrepare 收尾与下一子阶段

1. **P0 已完成首轮：补齐 `PrintkBuffer.setup()` 的 `InterruptType` wiring**。对照 Linux `setup_log_buf(0)` 的 `local_irq_save(flags)` / `local_irq_restore(flags)`，已修订 model/coding，使 `PrintkBuffer.Transition::Setup` 中切换 active printk buffer 和复制既有 records 的局部临界区在正式 local-interrupt context 内执行，并依赖已有 `BootCpuLocalInterrupt: InterruptType` 的 ready/disabled 状态。实现侧按保守策略执行 `SaveAndDisable` / `Restore`，保留 source context、绑定 `BootCpuLocalInterrupt` 的可观测事实和 ready-check 约束，不再只观察 `PrintkBuffer` 内部 summary bool。
2. **P0 已完成首轮：补齐 transition/action body 有序语义工具计划**。正式规格允许 `within Context { ... }` 只包住需要保护的局部操作，前后可以有 `drives { ... }` 或其它块；parser/AST/JSON/model 现在保留 source-ordered body member 序列，derive/check 按该序列处理 `drives` 与嵌套 `within`，并保持旧的分类字段兼容。`PrintkBuffer.Transition::Setup` 已用正式有序 body 表达 `setup_log_buf()` 的“前置动态缓冲准备 -> guarded active-buffer switch/copy -> 后续 remaining-record copy”路径，不再依赖注释说明局部保护范围。
3. **P0 已完成工具能力，后续按新策略暂缓使用：`only-once` 单次调用证明**。DSL 和 model 工具已支持 `within ContextName only-once { ... }`，并能从 `Kernel` 生命周期迁移的 `drives` 图出发计数验证，证明失败即报错。由于基于 Effective Context 的 guard 省略会增加当前启动阶段审计难度，新的 P0 策略会先从正式规格使用点移除 `only-once` 标记，保留工具能力和测试，不再把它作为近期 guard lowering 前置。当前需要记录以备未来恢复的四个使用点是：`spec/model/boot/core-prepare/phase.spec` 中的 `ResourceTreeWriteContext`、`CpuHotplugReadContext`、`StaticBranchJumpLabelContext`，以及 `spec/model/boot/entry-successor/phase.spec` 中的 `PrintkBufferSetupLocalInterruptContext`。
4. **P0 已完成首轮：复核 CorePrepare 剩余同步/并发面是否收口**。已完成 CorePrepare 已审计对象中的本地中断、抢占、自旋锁、Mutex、读写锁、RCU、per-cpu 同步、TLB/cache flush、CPU bring-up 同步量首轮复核；当前未保留未分类 P0 缺口。
5. **P0 后续：进入下一个启动子阶段**。CorePrepare 收口后切换到下一个 boot 子阶段，继续按“先 Linux 边界、再 model/coding、最后 impl 验证”的顺序审计。

### 当前执行计划：SLUB 收口后续计划

1. **P0 已完成：清理 trace、静态 SVG 和文档中的旧 SLUB facade 命名**。已把旧 facade 命名统一收敛为 `SlubSubsystem`，并用 `make verify REPORT=graph` 重新生成带 `state,transition` 注释的 `tools/out/trace/main.trace.svg`。`main.trace.svg` 已包含 annotation layer，且 `spec/charter/pic/**`、`spec/charter/**`、`docs/ROADMAP.md` 中的 SLUB facade 均使用 `SlubSubsystem`。
2. **P0 已完成复核：明确 `SlubSubsystem` 生命周期映射**。`preset/setup/enable` 分别对应 Linux `slab_state` 的 `DOWN -> PARTIAL -> UP -> FULL` 边界；当前 `mm_core_init()` 只推进到 `Ready/UP`，`kmem_cache_init_late()` 只建立 flush workqueue 事实，不推进 `Online/FULL`。实现中的 `SlubState` 已保留 `Full` 边界但不写入，`slab_sysfs_init()` / `SlubSubsystem.enable()` 保持后续项。
3. **P0 已完成首轮：收敛 named `SlubCache` 实例归属**。已以 `MmStructCache` 为参照，把 `PageTableLockCache("page->ptl")`、`VmapAreaCache("vmap_area")`、`RadixTree` node cache 和 `MapleTree` node cache 都改为通过 `SlubSubsystem.register_named_cache()` 注册到 `SlubCacheRegistry`，阶段对象只保存自身 kind/object-size 绑定和 ready fact；formal model、coding 约束、phase ready invariant 与 smoke 检查已同步。`process_prepare` 中尚未展开为具体阶段对象的 `vm_area_struct`、`vma_lock_cachep` 等专用 cache 继续保留在后续 proc/process-memory 计划，不在本轮提前塞入 `MmStructCache`。
4. **P1 后续：完整化 `KMEM_CACHE(...)` / named `SlubCache` 参数语义**。当前 named cache 注册只记录 `kind`、`object_size` 和 usercopy 区间，足以表达 owning subsystem 在对应 Linux 调用点创建了专用 cache，但不能长期代表完整 `kmem_cache_create()` / `KMEM_CACHE()` 语义。后续应扩展 `NamedSlubCache` 的 model/coding/impl 记录，至少覆盖 Linux cache name/kind、object size、alignment、flags、constructor presence、`SLAB_PANIC`/panic-on-fail 语义、RCU/type-safe/free 相关 flags、memcg/accounting/debug/hardening 相关 flags，以及 usercopy 区间；每个 owning subsystem 只在真实调用对应 `KMEM_CACHE(...)` 时填写这些参数，未进入当前阶段或当前配置裁剪的 cache 继续显式 deferred/trimmed。
5. **P1 已完成：收敛 KUnit KTAP 总数与动态 checkpoint 输出**。已修正默认 `APP=hello` KUnit 下 `user_boot` handler 的 fixture case count，限制 `virtio_blk` live-read submitted/completed 只把第一组动态 checkpoint 输出为 KTAP case，并让 `tools/test_summary.sh` 检测 subtest plan 与实际 case 数不一致。当前 `make test-kunit` 输出 `1..23` 且结束于 `ok 23`，`make test` summary 为 `KUnit checkpoints total=23 pass=23 fail=0`、overall `74/74`。
6. **P1 已完成首轮：`mm_core_init()` Linux 对照与固定检查面复核**。二轮复核已确认前序 G1-G7 高/中优先缺口基本关闭：`PageMetadataMap` 已前移到 `CorePreparePhase`，`MemoryTopology` 已收敛为 allocator-visible view，zonelist irqsave/printk-deferred、`MemoryDebugHardening` default-policy、SWIOTLB static area locks、vmalloc/ioremap sync contract 和 named `SlubCache` 归属均已进入 model/coding/impl/smoke。本轮已把 `PageExt`、`KFENCE`、`KMSAN`、`Kmemleak`、`DebugObjectsMemory`、`ExecMemory` 等按当前 Linux `.config` 裁剪的调用位置从纯文字 deferred 提升为 `MmCoreTrimmedPaths` 结构化观测，并修正 `execmem_init()` 的 no-op 依据为 `CONFIG_EXECMEM` 未选中。`StackDepot` 已按当前配置复核：`CONFIG_STACKDEPOT=y` 但 `CONFIG_STACKDEPOT_ALWAYS_INIT=n`，且当前 early 消费者未请求初始化，因此 `stack_depot_early_init()` 只记录调用点 passed/no early table allocation，完整 storage 与 save/fetch API 留给 late init 或未来消费者。固定检查面复核已形成 `/tmp/mm_core_init_fixed_surface_review.md` 矩阵记录；最新口径明确 `BootPhaseContext` 只能作为当前 boot call site 的上下文事实，不能作为规格省略锁/同步语义的依据。已补 model/coding facts，使 zonelist irqsave seqlock、printk-deferred、PageAllocator/SLUB runtime locking deferred、vmalloc/vfree guard/RCU/TLB flush contract 和 ioremap 继承关系保持可追踪；当前没有新的阻塞 gap。allocator/SLUB/vmalloc/ioremap 的完整 runtime 并发和跨 CPU flush 语义仍保留为 consumer-triggered deferred，不能写成已经完整完成。

后续计划：

1. 只在具体消费路径需要时展开 buddy allocator、SLUB、vmalloc/vfree、ioremap 等 runtime API 的锁、irqsave、preempt、RCU 和 TLB/cache flush 细节。
2. 若后续启用 page owner、kmemleak、KASAN、KFENCE、SLUB store-user 或 ref tracker，再回到 `StackDepot`，决定走 early request/hash table 建立，还是 late `stack_depot_init()`。
3. 进入下一个 boot 子阶段 `SchedInitPhase` 审计，继续按 Linux 调用边界、model/coding 规格、impl/smoke 观测的顺序推进。
4. 建立完整 `SeqLock` 规格建模与实现。当前 `ZonelistUpdateSeq` 只覆盖 `build_all_zonelists(NULL)` 的 boot-time writer guard；后续应抽象通用 `SeqLock` 类型，补齐 sequence counter、reader `read_seqbegin/read_seqretry`、writer enter/exit、irqsave variant 和必要内存顺序语义，并新增独立 smoke：用 `SeqLock` 定义一个新的测试实例，而不是复用 `ZonelistUpdateSeq`，覆盖 reader/writer/API 语义。
5. 为 `PrintkDeferredSection` / `printk_deferred_enter()/exit()` guard 增加独立 smoke。当前 `ZonelistPrintkDeferredSection` 只作为 `PageAllocator.Preset` 的调用点实例记录 enter/exit 配对；后续应使用同一 guard 类型定义一个新的测试实例，覆盖 enter/exit、重复 enter 拒绝、未 enter 直接 exit 拒绝和 active/ready 计数事实，避免只靠 zonelist 路径间接验证。
6. 除上述已由当前 gap 明确触发的 guard 原语外，并发原语不单独泛化扩张；只有当 `SchedInitPhase` 或后续消费路径实际依赖 Mutex/RwLock/RawSpinLock/RCU/InterruptType 的更细语义时，再作为对应 gap 处理。

### 暂缓讨论：transition/action 上下文需求声明

- **P2 待讨论：为对象 transition/action 定义上下文需求或能力声明机制**。该项暂不作为当前启动阶段审计的前置任务；它与 `within Context { ... }` 的边界还需要继续讨论，避免形成两套重复或不协调的上下文语义。近期仍以 `within Context` 和 guard 明确表达上下文来源；待 Mutex、RwLock、RCU、InterruptType 等 guard 规格实践稳定后，再回看是否需要 `requires_guard` / `requires_context` / `requires_capability` 等正式形态，用于表达 transition/action 对当前 Effective Context 的只读检查需求。
- **P2 长期：基于 Effective Context 的 guard 优化**。短期从正式规格使用点移除 `only-once` 并采用保守 guard lowering；长期可在基础规格稳定后恢复该优化。优化分两类：编译时优化可由 model 工具证明某个 lexical guarded block 单次可达、外层 Effective Context 已提供等价属性、且无 saved flags/token/owner/debug/memory-ordering 等协议副作用消费者时，生成 proof-only/elided guard；运行时优化可在保留 guard 对象和协议边界的前提下，根据实际 flags、嵌套计数、owner、CPU/task 状态或 target feature 做轻量 fast path。当前移除前需记录并保留的 `only-once` 恢复点为：`spec/model/boot/core-prepare/phase.spec` 中 `ResourceTreeWriteContext`、`CpuHotplugReadContext`、`StaticBranchJumpLabelContext`，以及 `spec/model/boot/entry-successor/phase.spec` 中 `PrintkBufferSetupLocalInterruptContext`。恢复该项前还应重新审查 `spec/model/SEMANTICS.md`、`spec/coding/objects/effective-context.md`、对应 phase coding `.md` 和工具测试，确保优化不改变规格语义，只影响可证明等价的 lowering。
