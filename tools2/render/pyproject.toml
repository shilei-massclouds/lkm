[project]
name = "lkm-render"
version = "0.1.0"
description = "Render tools2 structured Signal views as text."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
lkm-render = "render_tool.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
