[project]
name = "lkm-tools2-common"
version = "0.1.0"
description = "Shared protocol helpers for the isolated LKM tools2 toolchain."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
