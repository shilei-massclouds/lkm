[project]
name = "lkm-derive"
version = "0.1.0"
description = "Run bounded Signal derivation over tools2 model JSON."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
lkm-derive = "derive_tool.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
