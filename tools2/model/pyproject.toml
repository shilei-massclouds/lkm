[project]
name = "lkm-model"
version = "0.1.0"
description = "Build a tools2 Signal model from tools2 AST JSON."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
lkm-model = "model_tool.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
