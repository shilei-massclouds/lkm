[project]
name = "lkm-parse"
version = "0.1.0"
description = "Parse the isolated tools2 Signal DSL subset."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
lkm-parse = "parse_tool.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
