schema_version = 2
name = "df-0010-ltp-frontier-post-read-runqueue-linux-object"
description = "Repeat the exact interactive child history, frozen first-batch linux-object LTP supported selection, and second stock-script read that first exposed the child runqueue-publication failure."
mode = "stress"
test = "ltp-supported-post-read-lo"
runs = 50
classifier = "../classifiers/df-0010-ltp-frontier-post-read-runqueue-linux-object.toml"

[metadata]
defect = "DF-0010"
observation_mode = "ltp-frontier-post-read-runqueue-publication"
ordinary_path_required = true
default_suite = true
rootfs_profile = "canonical"
payload = "length check, failed sha256 child, interrupted partial tail command, host-matching sha256 and sh -n, exact first-batch supported selector, then a second sha256 child"
first_observation = "impl/arceos_ex/tests/basic/out/20260805T194115.149428Z-shell-lo-595228"
scope_note = "Preserve the runqueue-publication termination independently from the preceding stock-harness parse error and unsupported-syscall/TWARN boundaries; the identity must not follow the movable frontier, and successful samples do not erase the first failure."
